# 调试与修复规范（RVP 侧）

> 状态：生效中
> 建立：2026-08-06
> 目的：防止修复过程中责任错位、越界改动、上下文丢失、以及重复踩同一坑

---

## 0. 铁律（责任边界）

1. **所有问题的责任都在 RVP 侧**。本体 `ywzj_vehicle` 是被引用方，任何
   兼容 / 崩溃 / 功能问题，先假设是 RVP 侧引入，**禁止往"本体有问题、需要修本体"
   的方向思考**。历史上已多次因此走偏。
2. **永不修改本体**。`ywzj_vehicle/` 目录及其构建产物（jar）一律只读；
   需要本体配合时，用 RVP 侧的 mixin / 事件 / 接口 / 网络包 / 数据层解决。
3. 只允许修改 `d:\ywzj\ywzj\ywzj_rvp\` 内的文件（含本规范、docs、src、build 配置）。
4. 服务端运行测试在 `D:\ywzj\Forge-test-server` 执行（运行态产物，非源码修改），
   该目录不做任何源码级改动。

## 1. Mixin 纪律（默认拒绝）

新增任何 Mixin（含 `@Inject` / `@Redirect` / `@Overwrite` / `@Accessor` / `@Invoker`）
前必须逐级排除替代方案，有任一可行即不得用 Mixin：

1. **JSON 数据字段**：`RVP_*Data` 数据模型可表达的行为，优先在数据层配置。
2. **继承 / 接口 / 组合**：本体的可覆写方法、接口实现、`DistExecutor` 分端。
3. **Forge 事件总线**：`MinecraftForge` / `ModBus`（渲染 / Tick / 输入 / 网络注册事件）。
4. **网络包 + 独立管理类**：S2C / C2S 包 + `RVP_*` 独立系统。
5. **`@Accessor` / `@Invoker` 访问器**：仅需读私有字段 / 调 protected 方法时，
   放 `org.ywzj.rvp.accessor`，不新建 mixin 类。

只有确需修改本体 / 原版字节码且上述全不可行时才允许 Mixin，且：

- 改动面必须最小化：只允许最小 `@Inject`（HEAD / RETURN 转发），
  **禁止 `@Overwrite` 整方法、禁止整段 `@Redirect`**；确需改写核心逻辑时先向用户提出非 Mixin 替代。
- **服务端 dist 安全**：公共数组（`mixins`）的 Mixin 会把目标类改脏 →
  Forge 重算栈映射帧 → 目标类方法中若含 `@OnlyIn(CLIENT)` 类型引用
  （`LocalVehiclePlayer`、`Minecraft`、客户端粒子等）会触发服务端加载崩溃。
  精确判定（2026-08-06 与本体作者确认后）：危险的是"**非 `@OnlyIn(CLIENT)` 方法**中
  的客户端类型引用"——这类方法不被服务端 dist cleaner 剥离，LVP 引用留在字节码里，
  帧重算必崩；引用在 `@OnlyIn(CLIENT)` 方法里的类（如 `AmmoEntity`）会被剥离，安全。
  因此：公共数组 mixin 的目标类必须双端安全；`@OnlyIn(CLIENT)` 类型必须经
  `org.ywzj.rvp.client.bridge` 桥接（服务端 NOOP / 客户端真实现）。
- **RVP 侧 `@OnlyIn` 标注纪律**（作者建议落实）：RVP 公共类（非 `client` 包）方法引用
  任何 `@OnlyIn(CLIENT)` 类型（`LocalVehiclePlayer` / `Minecraft` / `ClientLevel` /
  `org.ywzj.vehicle.client.*` / `org.ywzj.rvp.client.*`）时，该方法**必须标注
  `@OnlyIn(Dist.CLIENT)`**，让服务端 dist cleaner 剥离；服务端会执行到的公共方法
  （`tick`、事件监听、网络包 handler）**绝对禁止**引用客户端类型。
- Mixin 类禁止持有共享状态、禁止非私有静态方法；业务逻辑放非 `mixin` 包辅助类。

## 2. 调试证据纪律

- **禁止任何 HTTP / 网络遥测**：不用 debug server、loopback、socket、webhook；
  运行时证据一律通过 Minecraft 命令或日志收集。
- 专用日志优先：`logs/<问题>.log`，不污染 `latest.log`。
- 每次复现必须记录：复现时间、mods 组合（哪些 jar）、启动命令、日志 / 崩溃报告路径、
  崩溃签名（首屏异常类型 + 前 3 帧）。
- 证据归档在 `docs/` 对应调研文档，供后续会话直接恢复上下文。

## 3. 文档记录规范（自动记录）

- 每个阶段的关键结论必须写入 `docs/` 下对应文档，**不能只留在对话里**。
- 变更（代码 / 配置 / 打包）前，先想清楚"这会导致什么功能丢失 / 降级"，并记入第 4 节。
- 每个修复步骤：`意图 → 改动点 → 预期影响 → 验证方式`，随做随记。
- 会话过长或上下文可能丢失前，优先把当前进度落盘。

## 4. 功能丢失 / 降级记录表

**格式**：日期 | 改动 | 丢失 / 降级功能 | 影响范围 | 恢复计划

| 日期 | 改动 | 丢失/降级功能 | 影响范围 | 恢复计划 |
|---|---|---|---|---|
| 2026-09-25 | **PS1SM 硬锁跨 512 格边界连续性**：同一目标在原生 `ClientLevel` 实体与本体 `serverEntities` 广播克隆间切换时实体 ID 相同但对象引用不同；本体 `RadarUnit`/`WeaponUnit` 先按引用检查、后按 ID 迁移，且 RVP 近距扫描会删除广播克隆，导致近→远出现接管空窗而脱锁。方案 A 在现有 `RVP_ClientRadarTickHandler` 中仅为当前硬锁 ID 保留待命克隆，在接触清理前按“存活本地实体→两个广播周期内的新鲜克隆”迁移探测条目、雷达硬锁和武器站目标，并将样本新鲜度校验推广到所有广播接触。零新增 Mixin，零本体/JSON/资源改动 | **有意行为修正，无功能降级**：同 ID 表示切换不再被当作目标消失；无有效替代对象时不迁移，手动解锁、箔条、真实销毁、出范围规则不变。新鲜度门会把停止广播后的远距鬼影限制在两个广播周期内 | 玩家驾驶的 RVP phase 雷达及其本体广播目标；重点为 PS1SM 锁定 F-14 跨 512 格边界 | 回退 `reconcileTrackedEntityRepresentations`、硬锁克隆保留分支及通用新鲜度校验即可恢复旧行为；实机矩阵见 `docs/plan/RVP机枪预瞄圈开发计划.md` |
| 2026-09-25 | **PS1SM 512 格外火控跟踪广播插值**：根因是本体 `serverEntities` 载具克隆每 5 Tick 同时改写 `xo/yo/zo` 与当前位置，且 `remoteTick()` 不推进；RVP HUD 已跨包插值，但导弹三态火控与机炮提前量仍读离散 AABB 中心。扩展 `RVP_ClientBroadcastVehicleInterpolator` 为 HUD/火控共享的中心+速度轨迹；RF 导弹 `STABLE/SEMI_AUTO` 改读共享中心；机炮广播目标不再使用不推进的 `entity.tickCount` 历史，弹道预测起点补回 `velocity×bufferTicks`，可视目标锚点仍保留缓冲中心；客户端采样独立于服务端 AHEAD 解算入口。零新增 Mixin，零本体/JSON/资源改动，零载具或武器 ID 硬编码 | **有意行为修正，无功能降级**：超远距火控沿用 HUD 的一个广播周期样本缓冲，换取无外推过冲的连续跟踪；机炮物理提前量单独补回该延迟。近距普通实体、雷达锁定、服务端 SACLOS PIP、AHEAD 引信和导弹权威制导不变 | 所有本体广播载具目标；重点为 PS1SM 2A38、TKB-1055、95Ya6M、Hermes 1A 的稳定/半自动火控 | 回退导弹执行器的共享样本消费和 `RVP_ClientMachinegunLeadResolver` 的广播目标分支即恢复旧行为。定向测试与完整 build 通过；服务端 `Done (2.844s)!`，对照基线无新错误，JVM 与 25565 已释放；实机矩阵见 `docs/plan/RVP机枪预瞄圈开发计划.md` |
| 2026-09-25 | **`rvp_rf` RVP导弹雷达硬锁视线完整三态 + SACLOS STABLE PIP**：复用现有客户端火控转发和鼠标重定向，新增本车/外置雷达正式硬锁解析；`STABLE` 严格跟目标中心并屏蔽瞄具鼠标，`SEMI_AUTO` 锁存相对目标方向的局部俯仰/方位偏置且受 `rvp_rf_off_axis_deg` 圆形限幅，`OFF` 完全手动。只认 `RadarUnit` 硬锁或服务端确认的外置锁，不认TWS/导引头软航迹；`STABLE + SACLOS + predict_target_pos` 时通过协议16同步请求，由服务端复核当前武器、RF站、请求新鲜度和硬锁后生成临时实体意图复用现有PIP，不写弹体持久 `targetEntity`；SEMI/OFF、丢锁或超时回退原视线制导，LBR/SALH/ARH不变。零新增Mixin、零JSON/资源/本体源码改动 | **有意扩展，无既有功能降级**：所有现有 `rvp_rf` RVP导弹统一获得三态；符合条件的SACLOS在稳定模式自动预测拦截。失去硬锁后立即停止驱动并清除偏置，SACLOS恢复视线制导。非导弹 `rvp_rf` 武器和机炮原行为保持；协议15客户端不能连接协议16服务端 | PS1SM的TKB-1055/95Ya6M，以及未来满足相同数据条件的 `rvp_rf` SACLOS导弹；三态仍覆盖Buk-M3、CSSA-5、IRIS-T SLM TEL等武器站 | 回退 `RVP_SACLOSStablePIPAssist`、照射会话/网络包新增字段和SACLOS实体意图分支，并将协议恢复15，即可仅撤销PIP扩展；三态本体回退点不变。验证：资格定向测试与完整build通过；服务端 `Done (2.321s)!`、无新增错误、进程与25565已释放；客户端实机矩阵见 `docs/plan/RVP机枪预瞄圈开发计划.md` |
| 2026-09-23～24 | **Mi-28 机炮自动提前量系统性偏后/偏下修复**：①解算、火控方向与 AHEAD HUD 从根 `sighting_system` 原点改为当前武器实际挂载 `auto_cannon` 炮口；②控制链保留世界提前点 EMA（alpha 0.23～0.46），按 `(1-alpha)/alpha` 理论滞后 tick 直接补偿 115%，匀速消除全部 EMA 滞后并额外前置约 0.18～0.50 tick，HUD 在相邻补偿控制解间插值；③目标速度改为最多 5 tick 中心实位移历史，空中目标以实位移为主、运动量为辅，着地目标不再混入悬挂/重力 `deltaMovement.y` 并将实位移 Y 限制为 ±0.25 格/tick；④复用 `weaponorigin verbose` 增加按需 `lead-solution` 快照。零新增 Mixin，零本体/JSON/结构模型/资源改动 | **有意行为修正**：保留 EMA 抗抖，匀速时宁可略提前也不保留 EMA 滞后；地面目标不再按物理引擎垂直速度外推；补偿随 EMA 点速度变化，不引入固定米数或目标朝向偏置 | RVP 机炮通用自动提前量，重点为 Mi-28/2A42 对固定翼及地面载具 | 115% 补偿定向测试通过；完整构建通过；服务端冒烟 `Done (3.533s)!` 且无新增错误。客户端实机矩阵与诊断方法见 `docs/plan/Mi28机炮自动提前量修复_20260923.md` |
| 2026-09-23 | **J-16D/PL-10 IR HMD 锁定圈变红但无锁定音修复**：对照 J-16/`j16_yj80` 有音效，确认两弹均回退同一 `IR_TRACK_ALARM`，非资源故障。根因为 J-16D `pl10` 武器站传感器默认 `NONE`，本体 `tickFireControl` 每 Tick 清掉 HMD 写入的共享 `WeaponUnit.lockedEntity`；HUD 已读 HMD 自有目标，但 `RVP_ClientSeekerTone` 仍读共享锁，且音效 Tick 早于 HMD 状态更新执行，导致两个真值源分裂。修复：IR HMD 活动时音效改读 `RVP_ClientHmdState.getLockedEntity()`，非 HMD 仍读本体共享锁；`RVP_ClientSeekerTone.tick()` 移到 HMD `checkIrHmd()/tick()` 之后，新锁/脱锁当 Tick 生效；删除同方法内重复的 `tone.isStopped()` 检查。零新增 Mixin，零本体/JSON/音效/模型资源改动 | **修复，无功能降级**：PL-10 IR HMD 锁定音与红圈、发射授权统一为 IR 通道状态；只有雷达硬锁时不误播 IR 音；YJ-80 与非 HMD IR 路径保持 | 客户端 RVP IR 发射武器锁定提示音，重点为 J-16D/PL-10；服务端、锁定物理门槛与弹体制导不受影响 | 回退 `RVP_ClientSeekerTone` 的 HMD 目标选择与 `RVP_ClientEvents` 的调用顺序即恢复旧行为（不建议）。验证：`./gradlew build` 成功；服务端 `Done (3.099s)!`，对比上一轮日志无新增错误，进程已结束且 25565 已释放；客户端实机矩阵待验。详见 `docs/RVP弹体-fish/RVP红外弹_20260921.md` 与 `RVP雷达HMD锁定_20260921.md` |
| 2026-09-22 | **J-16D/PL-10 双 HMD 锁定后 HUD 归属修复**：实机确认 Radar HMD 捕获成功且 K 面板有硬锁，但 PL-10 下不显示双绿色实心框；导引头关闭时 IR 红圈还会闪现一帧。根因有二：①雷达捕获同时写 `RadarUnit` 与共享 `WeaponUnit.lockedEntity`，`RVP_MissileOverlay` 未检查导引头/IR 通道所有权，误把通用火控目标画成 IR 锁；②`RVP_ScopeOverlay` 与本体雷达硬锁框均受 RF 传感器门控，非 RF 的 PL-10 即使实际硬锁也不绘制。修复：IR HMD 显示改读 `RVP_ClientHmdState` 自有目标并增加 seeker 门；`RVP_HmdOverlay` 对空/对地不再读共享锁；`RVP_ScopeOverlay` 抽取实际持锁 `RadarUnit` 的双绿框专用入口，非 RF HUD 只补画硬锁、不展开扫描航迹；雷达框和 IR 圈统一用插值后的包围盒中心锚点。零新增 Mixin、零本体/JSON/结构模型改动 | **修复，无功能降级**：导引头关闭时不再误闪 IR 圈；导引头开启时 IR 红圈与雷达双绿色硬锁框可并存；RF 红双圈、普通扫描航迹、外置雷达和 NCTR 既有门控保持不变 | RVP 飞机选择 IR 弹后使用 Radar HMD 的锁定后 HUD，重点为 J-16D/PL-10；捕获算法、发射授权、雷达探测和服务端行为不变 | 完整回退为还原 `RVP_HmdOverlay`、`RVP_MissileOverlay`、`RVP_ScopeOverlay` 本次三处改动。验证：`./gradlew test`、`./gradlew build` 成功；服务端 `Done (2.964s)!`，进程和 25565 端口已释放；A/B/C 客户端视觉矩阵待实机确认。详见 `docs/RVP弹体-fish/RVP雷达HMD锁定_20260921.md` 与 `RVP红外弹_20260921.md` |
| 2026-09-21 | **温压弹空爆尘环贴地门控修复**：温压视觉实例创建时采样爆心中心 X/Z 下方的碰撞地表；新增代码配置字段 `RVP_ThermobaricEffectInstance.MAX_DUST_RING_AIRBORNE_HEIGHT = 20.0F`。爆心离地高度严格超过 20 格时，跳过尘环地表预采样、环段数据创建和渲染；恰好 20 格仍生成；地表暂不可用时保留原行为。补充 20 格边界、超过阈值和未知地面的单元测试。 | **有意变更**：高空爆炸不再在地面投影生成贴地尘环；火球、压力波、凝结云和后燃烟云不受影响；阈值为 Java 代码配置，不增加武器 JSON 字段 | `rvp:thermobaric` / `rvp:thermobaric_standard` 温压视觉的贴地尘环 | 回退：将 `MAX_DUST_RING_AIRBORNE_HEIGHT` 调高，或移除 `shouldGenerateDustRing` 门控即可恢复旧行为。依据：`RVP_ThermobaricEffectInstance`、`RVP_ThermobaricRenderer` 与 `RVP_ThermobaricVisualCurveTest` |
| 2026-09-04 | DH 近距保护层由实体原点取光改为正常实体调度器取光，复用本体主碰撞盒中心探针；增加五秒限频取光对照 | 无新增功能丢失；按用户指定保留当前主体-only 范围，不恢复旧包额外几何 | 仅近距保护副本光照；真实环境亮度、摧毁暗化、远距代理及深度规则不变 | 完整构建与 378 项测试通过；`AADF4392…` 包已部署，用户以 Mi-28 原观瞄视角确认 LAV-AD/M142 目标材质恢复，实机验收通过，详见 DH 回归记录末节 |
| 2026-08-06 | 阶段1：从 `mixins.json` 公共数组移除 28 个「改脏带毒类」的 mixin（备份 `build/mixins_backup_20260806.json`） | ① **AbstractVehicle**（8 个 mixin）：APS 主动防护数据、ERA 反应装甲状态、炮手 chunk 加载、炮手数据同步、物理碰撞 OBB 初始化、联动 UAV、发射架部署、命中箱伤害系数 → 全部失效；② **WeaponUnit**（13 个）：武器舱访问器（bay 单元/当前武器索引/xTurnGroup）、臂式武器预选目标（`WeaponUnitArmExt`）、武器舱覆写、外部雷达锁定、发射架部署姿态绕过、跟随父节点旋转、组合挂架槽、待定雷达锁定、传感器类型覆盖、射击调试、发射架部署射击门控、多武器切换、武器切换数据同步 → 全部失效（RVP 武器服务端行为退化为本体原版）；③ **MissileEntity**（2 个）：导弹制导访问器、导弹 tick 扩展（子母弹/多段制导）→ 失效；④ **AbstractVehicleWeapon**（1 个）：炮手 AI 重装时间设置 → 失效；⑤ **RadarUnit**（1 个）：雷达单元扩展（tickTargets/tickDetect/toggle）→ 失效；⑥ **Radar**（1 个）：雷达信号特征（scanTargets/detectTargets/findTarget）→ 失效；⑦ **ServerHitVehicleEvent**（1 个）：命中车辆事件附加数据 → 失效；⑧ **FixedWingVehicle**（1 个）：固定翼自动起落架手动覆盖 → 失效 | 服务端武器/雷达/联动/装甲/炮手全链路功能降级；客户端视觉类（client 数组）不受影响 | 阶段2：不用 mixin 重写带毒方法（已否决 `@Overwrite`），改为**非 mixin 路径**逐功能恢复：继承覆写（RVP 实体/武器类已 extends 本体类）→ Forge 事件 → 网络包+独立管理类 → 数据层 JSON 字段；强转调用点先防御化（instanceof 降级）防 ClassCastException |
| 2026-08-06 | 阶段1 验证 | 服务端本体+RVP 启动成功 `Done (5.269s)!`（`test_phase1_20260806.log`，新 jar `ywzj_rvp-1.20.1-0.5.8-all.jar` 已部署 `mods/`，旧 jar 备份 `.bak_20260806`） | 启动链路 | 已知遗留风险：RVP 源码对已移除接口的**运行时强转**（`WeaponUnitAccessor`/`GunnerWeaponAccessorMixin`）会在开火/炮手/组装/调试命令路径抛 ClassCastException，需下一阶段防御化；`WeaponUnitArmExt` 类引用为 instanceof 判断，恒 false，静默降级不报错 |
| 2026-08-06 | 阶段1.5：`RVP_ExternalRadarLinkHelper` 3 个公共方法补 `@OnlyIn(CLIENT)` | 无（纯标注修复） | 客户端雷达链路 | 无 |
| 2026-08-06 | **B1（武器射击链路）恢复**：① 新建 `RVP_VehicleHurtScalingHandler`（`VehicleAttackEvent` + 反推输入伤害 + 取消重放 `hurt()` 恢复命中箱伤害系数，skip 表/重入守卫替代原 mixin 字段）；② 新建 `RVP_WeaponSensorHelper`（eo_ccip/override 传感器覆盖，客户端 `effectiveSensorType` + 双端安全 `effectiveOrStatic`）；③ `RVP_WeaponBase.noteServerShootInvocation` 恢复射击调试；④ 4 个武器 `shoot()` 接入调试 + ProjectileWeapon RF 判断用有效传感器；⑤ 客户端传感器消费点（ScopeOverlay/ClientEvents/RocketCcipOverlay/ClientRadarLockState/FireControlStabilizerState）接入 helper；⑥ 删除死文件：`AbstractVehicleHitboxDamageFactorMixin`/`WeaponUnitSensorOverrideMixin`/`WeaponUnitShootDebugMixin`/`ServerHitVehicleEventMixin`/`AbstractVehicleLauncherDeployMixin`/`WeaponUnitAccessor`/`RVP_VehicleHitboxRuntimeAccess`；⑦ 修复运行时强转地雷：`WeaponUnitAccessor` 全部引用改公共 API（`weaponBayUnits` public 字段 / `getCurrentWeaponIndex`/`getCurrentSecondaryWeaponIndex` public 方法；`xTurnGroup` 无公共 API → structureGroup/data 级近似）；`GunnerBrain` 的 `GunnerWeaponAccessorMixin` 强转改 `ObfuscationReflectionHelper` 反射防御化 | **已恢复**：命中箱伤害系数、火控传感器覆盖、服务端射击调试、武器舱挂架 map 读写（public 字段）、当前武器索引读取；**降级**：① `xTurnGroup` 实例访问（发射架部署姿态/自定义挂载渲染/射击调试输出用 `structureGroup` 或 `data.getRawXTurnGroup()` 近似，枢轴偏差可接受）② `ServerHitVehicleEventMixin` 命中伤害显示报文改写（D-1 永久降级）③ 无人机/驾驶员无限弹药 reload 强制归零改为反射（失败仅降级不崩） | 武器射击链路、发射架、自定义挂载 | 剩余批次 B2-B8 继续；xTurnGroup 若需精确恢复可后续评估非 mixin 反射/SRG 方案 |
| 2026-08-06 | B1 验证 | Mohist 服务端启动成功（11.156s，世界全维度加载完成，无 RVP 相关 ERROR/ClassCastException；日志 `E:\ywzj test server\ywzj test server\logs\latest.log`，jar 已重新部署） | 启动链路 | 待后续游戏内射击/伤害实机验证 |
| 2026-08-06 | **B2（武器切换与弹舱）恢复**：① 新建 `RVP_WeaponSwitchSyncHelper`（替代 `WeaponUnitSetWeaponMixin` + `WeaponUnitSwitchWeaponMixin` 锁定恢复部分）：每 tick 检测武器索引变化 → 自动同步弹舱（目标舱 on 其余 off）+ 手动覆盖推断 + RF 下雷达锁定恢复；② 新建 `RVP_FollowParentRotationHelper`（替代 `WeaponUnitFollowParentRotationMixin`）：每 tick 将 `follow_parent_only` 部件旋转重置回 baseRotation，保留 `RVP_HitboxDebug` 调试输出；③ `RVP_WeaponBase.tick()` 接入两个 helper（武器 tick 在本体 `super.tick()` 含 updateRot 之后执行，时序与原 mixin 等效） | **已恢复**：切换武器自动开关弹舱、手动 toggle 弹舱后自动同步暂停（启发式推断）、RF 传感器下切换后雷达锁定恢复、跟随父节点旋转；**降级**：① 弹舱同步用公共 `setOn`（比原 `setOnField` 多 `hasPower` 检查与驾驶员 chat 提示，语义更保守）② 纯本体武器（无 RVP 武器）的弹舱自动同步不触发 ③ `WeaponUnitModdingOnlyMultiMixin` 的 MULTI 循环拦截无法非 mixin 拦截（本体 `ClientVehicleSwitchWeapon` 包 handler 固定）→ 配置了 block 的载具 F 键多弹种循环仍会切换（B4 评估 RVP 索引映射抵消）④ 切换武器后 seekerOn 复位（SARH→SACLOS 残留）随 B3 反射卡点 | 武器切换/弹舱/跟随旋转链路 | 剩余 B3-B8 继续；MULTI block 如需精确恢复只能客户端 InputHandler mixin（client 数组，不脏服务端） |
| 2026-08-06 | B2 验证 | Mohist 服务端启动成功（10.853s，全维度加载完成，无 RVP 相关 ERROR/ClassCastException；日志 `E:\ywzj test server\ywzj test server\logs\latest.log`） | 启动链路 | 待游戏内切换武器/弹舱实机验证 |
| 2026-08-06 | **B3（锁定与预选）恢复**：① 新建 `RVP_WeaponLockStateTable`（side-table：ARM 预选 / 外部雷达请求与锁定 / 待定锁定三张静态 Map，替代 `WeaponUnitArmExt`/`WeaponUnitExternalRadarLockExt`/`WeaponUnitPendingRadarLockExt` 三个 Ext 接口）；② 15 个调用点改写（`RadarRoleHelper`/`WeaponBase`/`ProjectileWeapon`/`MissileEntity`/`ExternalRadarLinkHelper`/`ExternalRadarSyncService`/3 个 C2S 包/`GunnerExternalRadarController`/`GunnerWeaponSuitability`/`ClientArmState`/`ClientRadarLockState`/`RuntimeArmGuidanceSource`/`ServerDebugCommands`/保留 mixin `WeaponUnitTickFireControlMixin` 内部改调 side-table）；③ `RVP_WeaponSwitchSyncHelper.syncSeekerOn` 用 `ObfuscationReflectionHelper` 反射复位 `WeaponUnit.seekerOn`（`toggleSeeker` 为 @OnlyIn(CLIENT) 服务端不可用，B2 遗留卡点）；④ 删除 9 个死文件（`WeaponUnitArmMixin`/`WeaponUnitExternalRadarLockMixin`/`WeaponUnitPendingRadarLockMixin`/`MissileEntityMixin`/`MissileEntityAccessor`/3 个 Ext 接口/`MissileEntityArmExt`） | **已恢复**：臂式武器 ARM 预选（客户端选中 → side-table → 服务端落表）、外部雷达锁定请求/清除/同步、待定雷达锁定、切换武器后 seekerOn 复位（SARH→SACLOS 残留消除）、导弹中继雷达锁定读取；**降级**：① seekerOn 反射失败仅静默降级（Forge 已 setAccessible，极罕见）② ARM/外部雷达状态为进程内静态表，跨进程状态仍走网络包（与原 mixin 字段等价）③ MULTI block 仍待 B4 评估（索引映射抵消）④ 本体导弹 ARM 链路 D-2b 定稿确认 | 武器锁定 / ARM 预选 / 外部雷达联动链路 | 剩余 B4-B8 继续；seekerOn 若需去反射可评估 client 数组安全类方案 |
| 2026-08-06 | B3 验证 | Mohist 服务端启动成功（10.303s，全维度加载完成，无 RVP 相关 ERROR/ClassCastException，seekerOn 反射静态初始化未抛异常；日志 `E:\ywzj test server\ywzj test server\logs\latest.log`） | 启动链路 | 待游戏内 ARM 预选/外部雷达锁定实机验证 |
| 2026-08-06 | **B4（组合挂架）恢复**：① 新建 `RVP_GroupedSlotSpawnHandler`（`EntityJoinLevelEvent` 触发 assembler，客户端/服务端双端执行；此时本体 `initData()→createPartUnits()→combineAndInit()` 已完成，`partUnitsView` 从 `vehicle.getPartUnits()` 重建）；② 确认 4.4.2 待确认项：`RVP_GroupedWeaponSlotAssembler` 只写本体 **public final** 字段（`weapons`/`secondaryWeapons`/`independentWeapons`/`indexedWeapons`/`weaponBayUnits`），不写私有列表，非 mixin 重组可行；③ 删除死文件：`WeaponUnitGroupedSlotMixin` + `WeaponUnitModdingOnlyMultiMixin`（均已从 `mixins.json` 移除、无人调用；后者目标类为带毒 `WeaponUnit`，误注册即崩服务端） | **已恢复**：组合挂架（`merge_into_previous_slot` 槽位合并为 `VehicleMultiWeapons`，含子部件/雷达联动/弹舱绑定）；**降级/说明**：① 本体 `combineAndInit` 先执行生成默认武器实例，assembler 在 `EntityJoinLevelEvent` 时清空重建（双份实例，旧实例 GC 回收，无功能影响；触发时序比原 mixin 晚一个生命周期阶段，等效）② **MULTI block 评估结论**（B2 遗留）：本体 `ClientVehicleSwitchWeapon` 包 handler 固定，`cycleMultiWeapon` 拦截无法非 mixin 精确实现，配置了 block 的载具 F 键多弹种循环仍会切换；替代路径 = RVP 变体选择界面（`RVP_ModdingVariantScreen` + `C2SSelectModdingSubWeapon` 已存在，服务端校验 `isModdingOnlyMulti`），如需 F 键精确拦截只能 client 数组 InputHandler mixin（不脏服务端，暂缓）③ 挂架装载/分组效果需游戏内实机验证 | 组合挂架/多弹种分组链路 | 剩余 B5-B8 继续；MULTI F 键拦截如需恢复走 client 数组 InputHandler mixin |
| 2026-08-06 | B4 验证 | Mohist 服务端启动成功（11.427s，全维度加载完成，无 RVP 相关 ERROR/ClassCastException；日志 `E:\ywzj test server\ywzj test server\logs\latest.log`，B3 jar 备份 `.bak_B3`） | 启动链路 | 待游戏内挂架装载/多弹种分组实机验证 |
| 2026-08-06 | **B5（炮手）恢复**：① 新建 `RVP_GunnerVehicleTickService`（`ServerTickEvent` 5 tick，替代被删 `AbstractVehicleGunnerChunkLoadMixin` 的 tick TAIL 注入；Forge 1.20.1 无 `EntityTickEvent`，改用低频扫描 `level.getAllEntities()` 过滤 Gunner 驾驶载具，条件与原 mixin 一致：非 uav/非销毁/驾驶员为 `GunnerEntity`/距玩家 ≤96 区块，`EntityUtil.keepChunkLoaded` 当前位置+前方 16 格预加载）；② 同服务内完成阵营/航向同步（替代 `AbstractVehicleGunnerDataMixin` 的 writeData/readData 注入）：新建 `S2CGunnerVehicleSync` 网络包 + 客户端侧表 `RVP_ClientGunnerVehicleState`（按 entityId 存 faction/yRot/xRot，远端实体 `remoteTick()` 为空操作故应用一次即可保持，clientTick 清理过期条目），战术地图 `relationColorForVehicle` 的 `AbstractVehicleGunnerDataAccessor` 强转改为侧表读取；③ 新建 `RVP_WeaponBase.ywzj_rvp$setReloadTime(int)` 公共方法（替代 `GunnerWeaponAccessorMixin` invoker），`GunnerBrain.forceSetReloadTime` 改 `instanceof RVP_WeaponBase` 直调，本体武器保留反射兜底；④ 删除 4 个死文件：`AbstractVehicleGunnerChunkLoadMixin`/`AbstractVehicleGunnerDataMixin`/`GunnerWeaponAccessorMixin`/`AbstractVehicleGunnerDataAccessor` | **已恢复**：① Gunner 驾驶载具区块强载（原 tick TAIL → 5 tick 扫描，失效条件相同）；② 远程 Gunner 载具阵营（敌/友着色）与航向（战术地图图标朝向）；③ 炮手 AI 对 RVP 武器的重装时间设置（含无限弹药 reload 强制归零，RVP 武器直调/本体武器反射）；**降级/说明**：① 原 mixin 的 `writeData` 内 `teamName` fallback（`__rvp_enemy__`/`__rvp_friendly__`）无法注入 → 本体远程载具 team 机制对"无队伍但带 faction 的 Gunner"不再感知，RVP 战术地图由侧表补偿；② chunk 加载从逐 tick 变为 5 tick 扫描，时效略降（区块 ticket 有效期足以覆盖）③ 阵营/航向同步仅发给乘载具玩家（与原广播范围一致）④ 空快照会清除客户端过期条目 | 炮手 AI/远程载具 IFF/战术地图链路 | 剩余 B6-B8 继续 |
| 2026-08-06 | B5 验证 | Mohist 服务端启动成功（10.751s，全维度加载完成，无 RVP 相关 ERROR/ClassCastException；日志 `E:\ywzj test server\ywzj test server\logs\latest.log`，B4 jar 备份 `.bak_B4`；Create/TacZ 缺模组 WARN 与 bedrock 模型 ERROR 为环境噪音非本次改动） | 启动链路 | 待游戏内炮手位操作武器/远程载具阵营实机验证 |
| 2026-08-07 | **B6（载具防护）恢复**：① **ERA**：新建 `RVP_EraStateTable`（UUID→失效骨块 Set 侧表）+ `RVP_EraStateSavedData`（主世界独立存档）+ `RVP_EraStateEventHandler`（`EntityJoinLevelEvent` 恢复 / `EntityLeaveLevelEvent` 写回，替代被删 `AbstractVehicleEraStateMixin`），消费点 `RVP_VehicleHitboxFactorManager` 改查表，客户端经 `S2CVehicleEraState`→`RVP_ClientVehicleEraState`；② **APS**：新建 `RVP_ApsRuntimeManager`（cooldown/装填/扫描/延迟触发/拦截，`Map<UUID,ApsState>` 替代注入字段）+ `RVP_ApsStateSavedData`（int[4] 持久化）+ `RVP_ApsEventHandler`（join/leave/server tick 驱动），拦截复用 `ServerVehicleFire`，HUD 复用 `S2CApsHudSync`（替代被删 `AbstractVehicleApsMixin`）；③ **命中箱**：`RVP_VehicleHitboxFactorManager` 承接 B1 系数 + ERA 联动；④ **物理碰撞**：`RVP_PhysicsOnlyCollisionHelper`（WeakHashMap 侧表）+ `RVP_PhysicsOnlyCollisionEventHandler`（join 重建 / tick 更新 / leave 清理，双端，替代 `AbstractVehiclePhysicsOnlyCollisionMixin`）；⑤ **发射架部署**：新建 `LauncherDeployStateMachine` + `RVP_LauncherDeployEventHandler`（服务端+客户端每 tick 推进状态机与应用俯仰旁路，替代 `AbstractVehicleLauncherDeployMixin` + `WeaponUnitLauncherDeployPoseBypassMixin`）；射击门控移入 `RVP_WeaponBase.canShootOnServer()`（`passesLauncherDeployGate` 查 `RVP_LauncherDeployConfigCache`，替代 `WeaponUnitLauncherDeployGateMixin`）；⑥ **起落架手动覆盖**：`AutoLandingGearManualOverrideMixin` 目标从带毒 `FixedWingVehicle` 改为安全 `ClientVehicleAction`（拦截 `toggleLandingGear` 入包 → `AutoLandingGearManualOverrideManager`）；⑦ **联动 UAV**：新建 `RVP_LinkedUavStateTable`（UUID→State 侧表）+ `RVP_LinkedUavEventHandler`（ServerTick：残骸 10 秒过期/部署实例强载区块/母车位置快照与被毁联动；`EntityLeaveLevelEvent` 清理+回传母车；`EntityMountEvent` 上下车处理），承接 `RVP_DeployableUavService`/`RVP_UavLoiterTickService`（替代 `AbstractVehicleLinkedUavMixin`）；⑧ 删除死文件：`AbstractVehicleApsMixin`/`AbstractVehicleEraStateMixin`/`AbstractVehicleLinkedUavMixin`/`AbstractVehiclePhysicsOnlyCollisionMixin`/`WeaponUnitLauncherDeployGateMixin`/`WeaponUnitLauncherDeployPoseBypassMixin`/`ext/AbstractVehicleLinkedUavExt`/`ext/RVPEraStateAccess`/`ext/RVPPhysicsOnlyCollisionAccess`；⑨ 踩坑：Forge 1.20.1 无 `EntityLeaveWorldEvent`（正确类名 **`EntityLeaveLevelEvent`**，5 个事件处理器均改）；`Level.getEntities()` 为 protected，双端遍历改 `ServerLevel.getEntities()`/`ClientLevel.entitiesForRendering()` | **已恢复**：ERA 失效骨块持久化与消费、APS 主动防护全链路、命中箱+ERA 联动、物理碰撞专用 OBB、发射架部署/门控/姿态旁路、起落架手动覆盖、联动 UAV（父子/区块保活/被毁联动/回传母车）；**降级/说明**：① 发射架姿态旁路在 tick 末应用，与本体时序近似 ② UAV 座位锁为每 tick 轮询（替代原注入）③ APS HUD 同步每 10 tick 或值变化 ④ ERA/APS 持久化走独立存档（本体 NBT 无法注入） | 载具防护/联动链路 | 剩余 B7（雷达）、B8（收尾）继续；`RotaryWingAutoLandingGearOverrideMixin` 目标 `RotaryWingVehicle.onClientVehicleAction` 已静态验证无 `LocalVehiclePlayer` 引用（不带毒），纯 Forge 启动已实证安全 |
| 2026-08-07 | B6 验证 | 纯 Forge 服务端启动成功 `Done (5.925s)!`（`logs/latest.log`；启动命令 `java.exe @user_jvm_args.txt @libraries/net/minecraftforge/forge/1.20.1-47.4.13/win_args.txt nogui`，**Java 必须用全路径 `C:\Program Files\Java\jdk-21.0.11\bin\java.exe`，PATH 上 java 为 1.8 不支持 @argfile 与模块参数**）；无 RuntimeDistCleaner/mixin 错误（Create/TacZ 缺模组 WARN 与 bedrock 模型 ERROR 为环境噪音）；jar 归档 `stage-jars/ywzj_rvp-1.20.1-0.5.8-stage-B6-20260807.jar`，测试服旧包备份 `.bak_20260807` | 启动链路 | 待游戏内载具受击/ERA 消耗/APS 拦截/发射架部署/UAV 联动实机验证 |
| 2026-08-07 | B6 验证（Mohist） | Mohist 服务端启动成功 `Mohist 启动成功啦！总耗时共 16.610s`（`E:\ywzj test server\ywzj test server\logs\latest.log`；启动 `java -Xmx6G -jar mohist-1.20.1-46ca7304-server.jar nogui`，JDK 21 全路径；无 RuntimeDistCleaner/mixin 错误，仅 Create/TacZ 缺模组 WARN、bedrock 模型 ERROR、Yggdrasil 离线超时为环境噪音）；B6 jar 覆盖部署 `mods\ywzj_rvp-1.20.1-0.5.8.jar`，旧包备份 `.bak_B5` | 启动链路 | 待游戏内实机验证（同 Forge） |
| 2026-08-07 | **B7（雷达）恢复**：① **雷达行为**：新建 `RVP_RadarScanService`（`ServerTickEvent` END 每 4 tick，替代被删 `RadarUnitMixin.serverScanForGunner`）：遍历 `ServerLevel` 实体过滤「Gunner 驾驶 + 有电 + 未毁」载具 → 其各 `RadarUnit`（on）→ `Radar.scanTargets`（高度/方位限位/旋转速度/扇区过滤，与原 mixin 一致）→ `filterUndetectableRvpAmmo`（排除本体 `BulletEntity`/`RVP_BulletEntity`/不可探测 RVP 弹，复刻原逻辑）→ `appendRvpAmmoTargets`（`maxScanDistance × signatureSize` 缩放距离补入 RVP 弹体，复刻原逻辑）→ `radar.detect()` 写本体探测表；② **雷达信号特征**：原 `RadarSignatureMixin` 是 mixin @Redirect，非 mixin 恢复曾尝试 `RVP_BaseBullet.getBoundingBox()` 覆写虚拟箱 → **编译失败（`Entity.getBoundingBox()` 为 final 不可覆写，且 `tickHit()` 已被 RVP 自实现）→ 方案改由 `appendRvpAmmoTargets` 单一路径承担 RVP 弹体探测（本体 scanTargets 的 `getSize()<1` 过滤本就排除小体积弹体，虚拟箱必要性消除）；③ 删除死文件：`RadarUnitMixin.java`/`RadarSignatureMixin.java`（均未注册于 `mixins.json`）及临时 `_tmp_*.java`；`InfraredSignatureMixin` Javadoc 的 `{@link RadarSignatureMixin}` 改为 `{@code}`（防 javadoc 失效） | **已恢复**：① 炮手（`GunnerEntity`）驾驶载具服务端雷达探测表填充（武器锁定校验/导弹制导校验依赖 `getDetectedEntities()`）；② RVP 弹体被雷达探测/锁定（签名缩放距离）；**降级/说明**：① 扫描频率每 4 tick（原每 tick，本体接触保持最短 100ms 可覆盖）；② 客户端玩家驾驶雷达的 `tickDetect`/`tickTargets` 恢复为本体 vanilla 行为（原 mixin HEAD-cancel 的 phase 模式/自定义告警间隔不恢复），RVP 弹体在本体雷达 UI 的精确显示受本体同步机制限制（D-2a 第 3 层，感知等效）；③ `RadarUnitDataExt` 扫描参数（高度/周期/保持）仍经保留 mixin `RadarUnitDataMixin` 读取 | 雷达探测/锁定链路 | B8 收尾后全批次核对 |
| 2026-08-07 | B7 验证 | 纯 Forge 服务端启动成功 `Done (5.837s)!`（启动命令同 B6，JDK 21 全路径；无 RuntimeDistCleaner/mixin 错误，Create/TacZ WARN 与 bedrock ERROR 为环境噪音）；jar 归档 `stage-jars/ywzj_rvp-1.20.1-0.5.8-stage-B7-20260807.jar`，测试服旧包备份 `.bak_B6` | 启动链路 | 待游戏内雷达探测/来袭告警实机验证 |
| 2026-08-07 | B7 验证（Mohist） | Mohist 服务端启动成功 `Mohist 启动成功啦！总耗时共 10.846s`（`E:\ywzj test server\ywzj test server\logs\latest.log`；JDK 21 全路径；无 RuntimeDistCleaner/mixin 错误，仅环境噪音）；B7 jar 覆盖部署 `mods\ywzj_rvp-1.20.1-0.5.8.jar`，旧包备份 `.bak_B6` | 启动链路 | 待游戏内实机验证（同 Forge） |
| 2026-08-07 | **B8（收尾定稿）**：① **C 组反射卡点 8 处逐个评估**（详见 `修复方向/进度交接_20260806.md` 4.10.1）：仅 C1 `seekerOn`、C5 `setReloadTime` 实际使用反射（`ObfuscationReflectionHelper`，SRG 正确、失败静默降级不崩溃）；C2 `xTurnGroup` 改公共 `getStructureGroup()`、C3 `fakeOperatorPosition`/C4 `destroyedTime` 改侧表、C6 `guidance` 间接读、C7 `weaponUnit` RVP 弹体自持映射、C8 `SwitchableUnit.on` 用公共 `setOn()`（接受副作用）——6 处免反射；② 第 6 节强转风险点 6 个文件全部防御化（全工程无 `(WeaponUnitAccessor)`/`(GunnerWeaponAccessorMixin)` 强转残留）；③ **永久降级清单定稿 2 项**（#14 本体导弹 ARM → RVP 导弹已有 ARM；#28 命中显示 → B1 伤害系数吸收）；④ B1-B8 共 26 项功能全部非 mixin 恢复 | **阶段 2 完成**：26 项已恢复 / 2 项永久降级（#14、#28）/ 无悬空项；公共 mixin 数组 21 项无带毒目标 | 全量 | 双端回归通过，见下 |
| 2026-08-07 | B8 验证 | 纯 Forge 服务端启动成功 `Done (5.912s)!`（`D:\ywzj\Forge-test-server`；JDK 21 全路径；无 RuntimeDistCleaner/mixin 错误，Create/TacZ WARN 与 bedrock ERROR 为环境噪音）；Mohist 服务端启动成功 `Mohist 启动成功啦！总耗时共 10.855s`（`E:\ywzj test server\ywzj test server\logs\latest.log`；JDK 21 全路径；无 RuntimeDistCleaner/mixin 错误，仅环境噪音）；jar 归档 `stage-jars/ywzj_rvp-1.20.1-0.5.8-stage-B8-20260807.jar`（B8 与 B7 功能代码相同，为收尾定稿批）；测试服旧包备份 `.bak_B7` | 启动链路 | 待游戏内全功能实机回归 |
| 2026-08-07 | **B8 复查清理** | 删除 5 个未注册死 mixin 源文件（`WeaponUnitSetWeaponMixin`/`WeaponUnitSwitchWeaponMixin`/`WeaponUnitFollowParentRotationMixin`——目标均带毒 `WeaponUnit`，功能已被 `RVP_WeaponSwitchSyncHelper`/`RVP_FollowParentRotationHelper` 替代；`VehicleScopeOverlayMixin`/`VehicleRadarOverlayPositionMixin`——历史停用，已被已注册 `VehicleScopeOverlay*` mixin 与 `RVP_RadarOverlay`+`UIPresetManager` 替代）+ 误创建文件 `tatus --short` | 无（纯死代码清理；删除前已确认 60 注册类源文件齐全、无任何引用） | 无需恢复；验证：`compileJava` 0 错误，Forge `Done (5.575s)!` / Mohist `Mohist 启动成功啦！总耗时 10.613s`；jar 归档 `stage-jars/*-stage-review-20260807.jar`，旧包备份 `.bak_review` |
| 2026-08-07 | **phase 雷达客户端探测恢复 + rvpdebug spawn 放载具命令**：① 新建 `RVP_RadarScanHelper`（公共扫描工具：高度/方位环绕/扇区过滤 + RVP 弹体过滤与签名缩放补入）；② 新建 `RVP_ClientRadarTickHandler`（客户端 tick，玩家驾驶载具：phase 雷达改 `Radar.scanTargets` 全扇区探测 + RVP 弹体补入，非 phase 仅补 RVP 弹体，`scanPeriodTick` 节流复刻原 mixin，替代被删 `RadarUnitMixin#ywzj_rvp$tickDetect`）；③ `RVP_RadarScanService` 重写复用 helper（服务端炮手扫描，移除重复私有方法）；④ 新建 `C2SDebugSpawnVehicle` C2S 包 + `RVP_Network` 注册（id=33）+ `RVP_DebugCommands` 注册 `rvpdebug spawn <vehicleId>`（`ResourceLocationArgument`；无命名空间输入经 `ResourceLocationArgument` 解析为 `minecraft:xxx`，handler 加 `minecraft:`→`rvp:` 兜底） | **已恢复**：phase 雷达（`scan_animation_mode:"phase"`）玩家驾驶时全扇区目标探测与 RVP 弹体补入；新增 `rvpdebug spawn` 放载具调试命令（仅创造/OP）；**降级/说明**：① vanilla `tickDetect` 无法取消仍每 tick 执行（DETECT 网络包回写延迟一 tick 带上本类目标）；② phase 雷达节流按 `scanPeriodTick` 跳帧扫描（复刻原 mixin） | 客户端雷达探测链路 + 调试命令 | 待游戏内 phase 雷达扫目标实机验证；验证：`compileJava` 0 错误，Forge `Done (5.721s)!`；jar 归档 `stage-jars/*-stage-phase-spawn-20260807.jar`，测试客户端/服务端旧包备份 `.bak_phase` |
| 2026-08-07 | **phase 雷达接触保持恢复（扫描后保活）**：`RVP_ClientRadarTickHandler` 新增 `tickContactHold`（每 tick 刷新仍在扫描范围内目标的接触时间戳保活 + 移除出高度/方位限位/扇区、死亡、不可探测弹体，复刻被删 `RadarUnitMixin#ywzj_rvp$tickTargets` 客户端语义，替代仅时间寿命的 vanilla `tickTargets`）；phase 全扇区扫描仍按 `scanPeriodTick` 节流 | **已恢复**：目标被扫描到后在扫描间隙不再瞬间消失（扫描后保活），出扇区/高度/死亡即移除；**说明**：phase 雷达 `y_rot_speed=0` 时 vanilla `tickTargets` 寿命本为无限，保活刷新进一步统一出范围即移除语义 | 客户端雷达探测链路 | 待游戏内 phase 雷达持续跟踪实机验证；验证：`compileJava` 0 错误，Forge `Done (5.799s)!`；jar 归档 `stage-jars/*-stage-phase-hold-20260807.jar` |
| 2026-08-07 | **无人机离开母车视距后切不回/下不来修复**：`RVP_LinkedUavEventHandler#refreshParentPosition` 原实现存在鸡生蛋缺陷——母车实体被服务端卸载后（`serverLevel.getEntity(parentUuid)==null`）停止强载，母车区块永不重新加载 → `switchBackToParent`/`tryAutoRideParent` 找不到母车实体 → 无法切回母车、自动上车重试也失败。修复：母车实体不可用时按最近同步的位置快照（`RVP_DeployableUavService.getLinkedParentLastPosition`，key=无人机 UUID，母车卸载不会被误清）在 96 区块距离上限内继续 `keepChunkLoaded` 强载，区块重载后母车实体恢复 | **已恢复**：无人机飞离母车后母车区块持续强载，M 键切回与下车自动回母车在母车"离开视距"后仍可恢复；**说明**：超过 96 区块（1536 格）后仍不强载（设计上限） | 无人机部署/切换链路 | 待游戏内"无人机飞离母车再切回/下车"实机验证；验证：`compileJava` 0 错误，Forge `Done (6.237s)!`；jar 归档 `stage-jars/*-stage-uav-parentkeep-20260807.jar` |
| 2026-08-07 | **排查：强制退出崩溃**（crash-2026-08-07_03.16.03-server.txt，集成服务器登出崩溃） | 崩溃为 `LinkageError: log4j MessageSupplier loader constraint`，发生在 `firePlayerLoggedOut` → `EventBus.handleException`——登出时某 mod 的监听器抛异常，eventbus 记录日志时触发 log4j 版本/加载器冲突（用户整合包 Forge 47.4.3 + eventbus 6.0.5 + log4j 2.19.0），**原始异常被掩盖**；RVP 无登出监听器，非 RVP 引发 | 环境性（整合包 log4j 冲突）；建议：登出崩溃需在 E 盘整合包侧处理 log4j（或换 Forge 47.4.13），RVP 侧先修好无人机状态避免卡死状态下强退 | 非 RVP 功能 | 待用户更新整合包 log4j 后复测 |
| 2026-08-07 | **无人机切换后"一切冻结"诊断与修复（进行中）**：① 症状——切换到无人机后服务端响应极慢/停摆（怪物不动、难度改不了、M/J 无效），强退登出崩溃（log4j LinkageError 掩盖）；② 根因——`refreshParentPosition` 兜底强载在母车实体被卸载后**每 tick** `addRegionTicket(POST_TELEPORT, radius=3)` 强制重载远处母车区块，DistanceManager 每 tick 反复重排/加载 49 区块，开销大导致服务端 tick 停摆；③ 修复——兜底强载降频为每 40 tick（POST_TELEPORT ticket 有效期 300 tick 足够）；④ 新增诊断日志：`C2SSwitchDeployableUav.handle`/`switchBackToParent`（按 M 时车辆/UAV实例/母车解析/失败原因）、`refreshParentPosition`（每 100 tick 母车实体存在与否/快照位置/距离）、`handleDismount`（离机回传） | 待实机验证：切换后是否不再冻结、M 切回/下车是否恢复 | 无人机部署/切换链路 | 待用户用带日志 jar 复现；验证：`compileJava` 0 错误，Forge `Done (5.895s)!`；jar 归档 `stage-jars/*-stage-uav-diag-20260807.jar` |

| 2026-08-07 | **纯物理骨骼载具"放不出来"——根因确认并修复**：诊断 jar（stage-physdiag）实测日志确认 NPE 根因——`EntityJoinLevelEvent` 早于 `initData()`（服务端 `onAddedToWorld`→`initData`、客户端 `readSpawnData`→`initData` 都在该事件**之后**），事件里直接 `rebuildPhysicsOnlyCubes` 会在 `updatePhysicsOnlyCubes` 访问 `vehicle.centerOffset`（null）处 NPE，异常传播使 `addFreshEntity` 失败 → 带 `physics_only_bone` 的载具放不出来（无该参数的载具走 `hasPhysicsOnlyBones()==false` 提前返回，永不触发）；**修复**：重建改到每 tick（`ServerTickEvent`/`ClientTickEvent`）遍历时执行，以 `centerOffset != null`（initData 完成标志）为门槛一次性重建；重建标志用 `WeakHashMap<AbstractVehicle,Boolean>`（非 UUID——单机下服务端/客户端实体 UUID 相同会互相误判跳过，且弱引用自动清理）；`RVP_PhysicsOnlyCollisionHelper.rebuildPhysicsOnlyCubes` 保留 try/catch 防御。**验证**：dev runClient 实测 t90m/lav25 服务端+客户端均重建成功（physicsOnlyCubes=1、bodyCubes 剔除 21/13 正确，无 NPE）；另确认 `TickEvent.EntityTickEvent` 在 Forge 1.20.1 不存在（编译失败，改 Server/ClientTick 遍历） | 无（功能完整恢复：physics-only 盒重建+每 tick 更新+离开清理全保留；唯一延迟为加入世界后 1 tick 内完成重建） | 纯物理骨骼载具生成链路 | 验证：`compileJava` 0 错误，`BUILD SUCCESSFUL`；jar 归档 `stage-jars/*-stage-physfix-20260807.jar`，测试客户端旧包备份 `.bak_physfix` |
| 2026-08-07 | **无人机 M 键切换无限递归崩溃修复**：根因——M 键 `switchBackToParent` → `player.startRiding(母车)` → `startRiding` 内部检测玩家仍骑在无人机上自动 `stopRiding(suav)` → 触发 dismount 事件 → `handleDismount` → `tryAutoRideParent` 发现玩家 `getVehicle()` 仍是 suav（移除未完成）再次 `startRiding(母车)` → 同 tick 无限递归（日志 13233 次 `[RVP-UAV-DIAG] handleDismount` 刷屏），最终某实体掉虚空 discard 踢乘客触发监听器异常，被 log4j MessageSupplier **LinkageError** 掩盖成"登出崩溃"；**修复**——`tryAutoRideParent` 增加防重入守卫（`player.getVehicle() != null && != parent` 时跳过自动上车，由最外层显式 `startRiding` 完成骑乘）；同时清理 `handleDismount` 的 40 帧诊断堆栈日志（定位目的已达成）；**mixin 方案验证无效后删除**——`EventBusHandleExceptionMixin`（曾试注入 `EventBus.handleException` HEAD 打印原始异常+cancel 绕过 log4j）因 EventBus 由 modlauncher plugin service 提前加载、mixin 无法转换（output.log 无应用记录、System.out 验证输出 0 次）而放弃，源码文件已删且从未注册于 `ywzj_rvp.mixins.json` | **已恢复**：M 键切子载具/切回母车、下车自动回母车不再递归刷屏、不再崩溃；**说明**：强退登出时的 log4j LinkageError 为整合包环境冲突（前条目已记录，非 RVP 引发） | 无人机部署/切换链路 | dev runClient 实测多次 M 切换全部成功（`结果=true`）、「tryAutoRideParent 跳过」拦截 4 次、无刷屏无崩溃；纯 Forge `gradlew runServer` → `Done (14.248s)!` 启动成功、run/server 无 crash-reports、无 RuntimeDistCleaner/mixin 错误；jar 归档 `stage-jars/ywzj_rvp-1.20.1-0.5.8-stage-uavrec-20260807.jar` |
| 2026-08-07 | **服务端环境客户端 RVP 配置为空根因修复（挂架 + 改装工具，网络包方案）**：专用服务器下客户端进程不触发 `AddReloadListenerEvent`（服务端专属事件），`VehicleDataManagerMixin`（注入 apply）与 `RVP_VehicleExtendedConfigManager`（AddReloadListenerEvent 注册）在客户端均不运行 → 自定义挂架配置（`RVP_CustomMountConfigCache`）与改装工具弹种配置（`RVP_VehicleExtendedConfigManager.configs`）在客户端全为空 → 挂架不渲染、`RVP_ModdingScreenHook` 因 `getModdingOnlyEntries` 为空隐藏「RVP Variants」按钮；**修复（最终方案，零 mixin）**：服务端 `RVP_VehicleConfigSyncHandler` 监听 `OnDatapackSyncEvent`（与本体 `CommonAssetsManager.onDatapackSync` 同一事件同一时机），把含 RVP 字段的载具 JSON（过滤：挂架缓存 + 弹种配置的载具并集，gzip 压缩）通过 `S2CVehicleRvpConfig` 包下发，客户端收到后复用同一套解析逻辑填充两种配置；`RVP_VehicleExtendedConfigManager` 新增 `applyFromJsonMap` public 入口 + `apply` 时保存原始 JSON（`getRawVehicleJson`）供服务端过滤载荷；**废弃方案**：曾新建 `VehicleDataManagerFromNetworkMixin`（注入 `VehicleDataManager.fromNetwork` TAIL）实现同样效果，按项目规范「能用公共 API/自实现逻辑就不用 mixin」已删除，改为网络包 + 独立管理类；**降级/说明**：① 仅覆盖挂架与弹种两类配置，heat/发射架/APS 等其他 RVP 配置在客户端仍为空（对应功能均为服务端判定，客户端空不影响）② 改装工具失效与此同根因（非 mixin 误伤），随本次一并修复 | 自定义挂架渲染、改装工具弹种切换 | 待游戏内挂架显示 + 改装工具切弹种实机验证 |
| 2026-08-07 | **cssa5 导弹导引头 UI 残留修复**：`RVP_WeaponSwitchSyncHelper.tick` 无弹舱分支（`weaponBayUnits.isEmpty()`）原先直接 return，不调用 `syncSeekerOn` → cssa5（HQ13 导弹与 ahead 机炮同属 turret 武器站、无弹舱）从导弹切到机炮后 `seekerOn` 保持 true，导引头 UI 残留；**修复**：无弹舱分支改为调用 `syncSeekerOn(unit, 主索引, 副索引)`，非 seeker 武器切换后自动复位；同时 `seekerOnField()` 增加 `FMLEnvironment.dist != Dist.CLIENT` 直接返回 null（服务端不再尝试反射 `WeaponUnit`，避免连带加载客户端类 `VehicleSound` 触发 RuntimeDistCleaner 且失败后每 tick 重试刷屏） | **已恢复**：无弹舱武器站（cssa5 等）切换后导引头状态正确复位；服务端 seekerOn 恒 false（行为无差异） | cssa5 武器站切换链路 | 待游戏内 cssa5 导弹→机炮切换导引头 UI 实测 |
| 2026-08-07 | **ahead 弹药机制澄清与门控移除**：ahead 弹药的锁定**仅是装订空爆引信**（增强：弹丸在目标前空爆释放 ahead_cbc 弹幕），**不是发射前提**——未锁定/未装订时弹丸照常发射，像普通机炮一样直射（命中造成直击伤害）。曾误加 `passesAheadShotGate` 门控（未装订拒绝发射）导致"必须锁定才能发射"（cssa5 直接无法开火），已**完全移除**（shoot/shootBurstRound/fireBurstVolleyShot 三处调用 + 方法 + import 回退）；`RVP_AirburstInput` 的 ahead 排除恢复（R 键对 ahead 走 `fireControlLock` 锁定装订，不走测距；`programForShot` 每次射击 set 装订距离，无锁定时 set(0) 弹丸直射，符合"不锁定=普通机炮"语义） | **已恢复**：ahead 弹药任何时候可开火；锁定 → 空爆弹幕；不锁定 → 直射 | ahead 机炮发射链路 | 待游戏内 cssa5 ahead 锁定/不锁定两种发射实测 |
| 2026-08-07 | **ahead 服务端无法开火（RuntimeDistCleaner 崩溃）修复**：`RVP_AheadProgrammer.solve` 原调用 `RVP_MachinegunLeadSolver.resolveTrackedTarget`，该方法体引用 `LocalVehiclePlayer`/`Minecraft` 等客户端专属类——专用服务器执行到该分支抛 `Attempted to load class LocalVehiclePlayer for invalid dist DEDICATED_SERVER`（run/server 日志每次射击刷屏），ahead 武器服务端完全无法发射（装订后也打不出）；**修复**：`solve` 改用服务端安全的 `resolveTrackedTargetServerSafe`（仅查 `weaponUnit.getLockedEntity()` + `getMainRadarUnit().getLockedEntity()`，双端通用 API）；客户端 lead 提示仍走客户端版 `resolveTrackedTarget`（含本地外雷达锁定增强），互不影响；`solveForTarget` 仅用纯 MC API，服务端安全，保留 | **已修复**：锁定目标后 ahead 正常解算发射；未锁定 + `ahead_require_lock` → `lock_required` 拒绝不耗弹；服务端日志不再刷 RuntimeDistCleaner 异常 | ahead 机炮服务端发射链路 | 服务端日志确认 22:47 后无 RuntimeDistCleaner 刷屏 + 游戏内锁定发射实测 |
| 2026-08-07 | **T90M F 键弹种切换回归修复（grouped slot carrier）**：T90M 主炮 = AP 弹种组（3BM59/9M119，`modding_only_multi`）+ HE 弹种（3of26，`merge_into_previous_slot`）；期望 F 键在 AP↔HE 大组间切换、AP 组内用改装工具选弹种。原 `shouldBlockRuntimeMultiCycle` = `isModdingOnlyMulti && !isGroupedSlotCarrier` 对 grouped carrier 放行 → F 键 `cycleMultiWeapon`（本体语义=当前 multi 内 `cycleSubWeapon`）直接在 AP 组内 3BM59↔9M119 间切，绕过改装工具；**修复**：① `shouldBlockRuntimeMultiCycle` 改为拦截**所有** `modding_only_multi`（含 grouped carrier）② 新增 `shouldRedirectCurrentMultiCycle`（当前槽为 grouped slot carrier 时）③ `WeaponUnitMultiWeaponSwitchBlockMixin` 拦截 MULTI 后对 grouped carrier 重定向 `self.switchWeapon(false, next, false)`（武器槽切换）→ F 键在 AP↔HE 间切，组内子武器仅改装工具可选；lav25/zbl08a 纯 modding（非 grouped）拦截后不重定向，F 键无反应，改装工具不受影响 | **已恢复**：T90M F 键 = AP↔HE 大组切换、改装工具 = AP 组内 3BM59/9M119；lav25/zbl08a 改装工具正常 | T90M 主炮切换链路（服务端 + 客户端） | 待游戏内 T90M F 键大组切换 + 改装工具组内选弹种实测 |
| 2026-08-07 | **无人机服务端切换链路修复（双座占用/切回卡死/观瞄残留）**：① `RVP_DeployableUavService.tryAutoRideParent`：dismount 事件中玩家仍骑在无人机上（`player.getVehicle()==uav`，Forge removePassenger 先触发事件后置空 vehicle）时**同步** `startRiding(母车)` 会嵌套触发旧坐骑 stopRiding→再次 dismount 事件→递归返回后外层 stopRiding 因 `getVehicle()!=this` 守卫失败但 `onLeaveVehicle`/`addPassenger` 仍执行→**母车 onEnterVehicle 二次执行占两个座位**（座位栏两槽显示同一玩家）+ 无人机乘客列表残留幻影乘客；改为延迟到下一 tick（`scheduleAutoRide`，removePassenger 完成后 `player.getVehicle()==null` 再自动上车）② `switchBackToParent`：母车实体不可用（区块卸载/被移除）时原 return false 卡死玩家在无人机上，改为 `player.stopRiding()` 下车由 `handleDismount` 回传母车旁 + 进入延迟自动上车队列（母车区块重载后自动骑乘）③ `switchBackToParent` 成功路径显式 `clearSeatLock`（延迟自动上车任务会因玩家已骑乘被丢弃，原 `restoreSeatAndUnlock` 不再执行，避免座位锁残留）④ 新建客户端守卫 `RVP_ClientVehicleViewGuard`：每 tick 比对 `LocalVehiclePlayer.instance.getVehicle()` 与 `player.getVehicle()`，持续不一致超 40 tick（M 键切换正常窗口约 1-2 tick）→ `toSeat(null, null)` 强制清空观瞄状态，修复 /kill/自杀后车辆已移除但 `LocalVehiclePlayer.vehicle` 仍指向旧车导致的 zbl08a 观瞄视角残留 | **已修复**：① 两个座位栏目不再同时显示同一玩家（双座占用消除）② 飞离母车视距再切回不再卡死在无人机上（自动下车+区块重载后自动上车）③ 座位锁不再残留 ④ 车辆被移除/玩家离车后无观瞄视角残留；**说明**：母车区块重载前的过渡期玩家处于"未在载具"状态（M 键提示未在载具属正常） | 无人机部署/切换链路 + 客户端观瞄状态 | 待游戏内五个症状实机回归：切回可下车/座位不重复/观瞄与控制座位一致/离车无观瞄残留/母车飞出视距切回自动上车；验证：`compileJava` 0 错误，`BUILD SUCCESSFUL` |
| 2026-08-07 | **core_distance_scale_multiplier 失效修复（RVP 弹体/激光直击路径）**：根因——RVP 弹体/激光命中载具时已自行结算命中箱系数（`RVP_VehicleHurtScalingHandler.pushSkip` 跳过全局缩放），但 handler 在 `shouldSkip` 时**整体 return**，核心距离倍率从未应用；最终 `entity.hurt` 仍进本体 `DamageSystem.hurt`，其自带核心距离衰减（`amount >= damageThreshold` 时 `amount *= (distanceMax-distanceToCore)/distanceMax`）照常生效 → `core_distance_scale_multiplier: 0.0`（m142 等）完全不生效，命中点离核心越远伤害越低依旧；**修复（零 mixin）**：`RVP_VehicleHurtScalingHandler` 新增两个静态工具——① `resolveBaseFalloffScale`：按与本体 `DamageSystem.hurt` **完全一致的算法**（`closestHitObbPosition` 命中点 → 主 OBB 核心距离比例）计算本体会应用的衰减系数；② `compensateCoreDistanceFalloff`：`amount' = amount / falloff * (1 + (falloff-1)*coreMult)` 预补偿传入伤害量，使本体再次应用衰减后恰好等于期望值（coreMult=0 → 完全抵消衰减=命中点无关伤害；coreMult=1 → 原值=本体原衰减；低于 damageThreshold 本体走 0.1 下限分支不应用衰减，原样返回）；`RVP_BaseBullet.applyEntityHitDamage` 与 `RVP_LaserWeapon` 在调用 `EntityUtil.hurt` 前应用预补偿（激光 direct=射手非投射物，本体恒走 hitPos==null → scale=0.2 分支，按 0.2 补偿）；近炸/爆炸伤害走爆炸源本体不衰减，不受影响 | **已修复**：RVP 弹体/激光命中 m142 等配置 `core_distance_scale_multiplier: 0.0` 的载具时，命中点离核心远近不再影响伤害（直击全额）；默认 1.0 的载具行为不变 | 载具受击伤害链路（RVP 弹体/激光直击） | 待游戏内用 RVP 武器分别命中 M142 车头（近核心）与车尾/翼板（远核心）对比伤害，`HBX` 调试消息 damage 应一致；验证：`compileJava` 0 错误，`BUILD SUCCESSFUL` |
| 2026-08-29 | **炮兵地图偏航优先瞄准修复**：根因——逆 CCIP 解算后直接调用本体 `WeaponUnit.aim(...)`，M142 父 `seat` 偏航和子 `launcher` 俯仰同时追踪；父级未到位时子级局部角随父级旋转变化，同时鼠标移动把 `solution` 清空，限频解算间隙又会回落到普通视线瞄准，最终表现为俯仰上下跳动。**修复（零 Mixin）**：`RVP_ArtilleryFireControlState` 增加目标版本与 `PitchGate`；首次等待时递归捕获当前实际俯仰，连续移动不重捕获；未取得最新解时仍按最新地图点驱动偏航并保持俯仰；最新解就绪且偏航最短环绕误差 ≤0.5° 后才调用本体递归瞄准释放俯仰；分轴阶段复用公开 setter、`updateWorldAimVec()` 与既有 `ClientVehicleAction` 同步；无可动偏航轴时旁路；`Snapshot` 拒绝显示旧版本解 | **已完成代码与自动化边界验证**：不增加射击门控，不改网络协议/JSON/本体/结构模型；主类和测试类以 JDK 17 手工 `javac` 编译 0 错误，新增 7 个门控测试全部通过。**待验证**：M142 横向/纵向/斜向连续移动目标和高低弹道切换实机回归 | 炮兵地图逆 CCIP 自动瞄准、M142 父子武器站旋转与客户端/服务端角度同步 | 完整 `./gradlew build` 已按规定命令及无沙箱方式重试，但当前执行环境在任务启动前均报 `java.io.IOException: Unable to establish loopback connection`，未进入 Gradle 编译；需在正常本机终端补跑构建。实机结果按 `行为测试指南` C12 回填 |
| 2026-09-06 | **雷达搜索告警重构**：①新增 client 数组 `WarningReceiverSearchWarnMixin` 截流本体 `WarningReceiver.handle` 的 RADAR_SEARCH 分支（本体每 20 tick 固定一响且独立扫描无高度/扇区门控，导致 1 秒 1 响与 RWR 文字随 1000ms 淡出窗每秒闪烁）；②`S2CRvpWarn` 加 `audible` 字段，`RVP_WarnRelayService` 按敌方雷达扫描周期调度响声（首次接触立即响 + 每轮 `scan_period_tick` 响一声，非 phase 雷达 20t parity，出探测表/关机停响）；③`RVP_RadarOverlay` 机型文字淡出改平顶（600ms 恒全亮）；④删除 RVP 机械扫描支持：`RVP_ClientRadarTickHandler` 非 phase 雷达完全不干预（不补 RVP 弹体/接触保活，纯本体行为），`scan_animation_mode` 参数保留、phase 仍是唯一 RVP 路线开关 | **丢失/语义变化**：①本体原生载具（非 phase 雷达）雷达上不再显示 RVP 弹体接触、无 RVP 接触保活（回到纯本体行为）；②被照射者处于敌方雷达真实扫描体积外（低于 `scan_min_height`、俯仰扇区外）不再收到假"被搜索"告警（不响不闪）；③本体每秒固定搜索音消失，改按扫描周期（F-14 2.5s、cssa5 1s/3s、本体 1s） | 全部载具的 RADAR_SEARCH 告警与 RWR 文字显示；未配置 phase 的雷达客户端探测 | 回退：从 `ywzj_rvp.mixins.json` client 数组移除 `WarningReceiverSearchWarnMixin` 即恢复本体原始告警链路；恢复机械扫描支持则还原 `RVP_ClientRadarTickHandler` 的非 phase 分支。方案详见 `docs/plan/RVP雷达搜索告警重构方案_20260906.md` |
| 2026-09-07 | **dev runClient 启动崩溃修复（其他开发者反馈"mixin 报错"）**：三个独立问题——①本体 0.5.10 适配以来 ApricityUI 仅 `compileOnly`，dev 运行时类路径缺失 → `EventSubclassTransformer` 解析 `ApricityScreen` 父类失败 → CONSTRUCT 崩溃（0902 旧日志已存在，非本次改动引入）；②改 `runtimeOnly` 后 AUI 生产版 SRG jar 进 dev named 环境 → `NoSuchMethodError(MenuScreens.m_96206_)` DeferredWorkQueue 崩溃；③最近提交的 `VehicleAimAtOverlaySeekerColorMixin` 准星样式覆盖 `@WrapOperation(method="render")` 目标错误——本体对 `WeaponUnit.crosshairStyle` 的唯一 getfield 在 `lambda$render$2` 内（javap 实证），`render` 主体无此字段读取 → `(0/1) succeeded` InjectionError 崩溃（该提交标注"待实机验证"实际从未在 dev 验证过，一进客户端即崩） | **修复**：①AUI 改 `implementation fg.deobf('com.sighs:ApricityUI-forge-1.20.1:1.2.4')`（编译+dev 运行时双满足，正式环境仍由本体 jarJar 提供）；②准星覆盖注入目标改 `method="lambda$render$2"`（同文件其余 5 个注入均为此写法）；③顺带发现 `run/client_1` 载具包副本已偏离仓库（`suav.animation.json` 仓库包不存在而 run 包存在且解析失败） | dev 环境 runClient 启动链路；`crosshair_style_override` 功能此前从未实际生效（一加载即崩） | runClient 实测到达主菜单（`Sound engine started`，0 mixin 错误/0 FATAL/无新 crash-report）；`run/client_1` 载具包需按规范全量重拷仓库版（遗留维护项） |
| 2026-09-13 | **有人载具潜行右键改装屏蔽（用户要求的有意行为变更）**：新增 `org.ywzj.rvp.event.RVP_ModdingInteractGuard`（Forge 事件 `PlayerInteractEvent.EntityInteract`，`HIGHEST` 优先级，双端，零 Mixin）——手持本体改装工具 + 潜行 + 主手 + 目标 `AbstractVehicle` + 本体公共射线（`VectorUtil.hitPartUnit`，与本体同源 4 格）命中 `WeaponUnit.isInteractive()` 且 `RVP_VehicleExtendedConfigManager.hasPilotOrGunnerPassenger`（新抽取的公共判断，`canModVehicle` 改为复用）为真时 `setCanceled(true)`，本体 `AllEvents`（NORMAL、不收已取消事件）被跳过 → `WeaponUnit.onInteract → switchWeapon` 不再执行，动作栏提示 `rvp.modding.vehicle_occupied`；**同日二次迭代**：新增 client 数组 `VehicleOverlayWeaponListSuppressMixin`（用户批准的唯一新增 Mixin）——`@Inject(renderWeaponList, HEAD, cancellable)`，载具有玩家/gunner 乘员时取消绘制武器改装列表 UI（空手/任何物品、步行/骑乘均生效；目标为本体纯客户端类 `VehicleOverlay`，非带毒公共类） | **有意屏蔽**：①有人（玩家/gunner 乘员）载具的车外"潜行+主手+改装工具"右键子武器站换弹——本体该链路原本无乘员校验（调查确认：距离限 4 格、判定全在本体，RVP 原本无任何该链路代码）；②有人载具对准子武器站的武器改装列表 UI 不再显示（空手也生效） | 场景①仅五条件同时满足；场景②仅"有人载具+对准可改装子武器站"；停放无人载具改装与列表、未潜行右键开屏/开门、修理工具、门/装饰等其他部件交互、骑乘自身驾驶 HUD 均不受影响 | 回退：删除 `RVP_ModdingInteractGuard` + 从 `ywzj_rvp.mixins.json` client 数组移除 `VehicleOverlayWeaponListSuppressMixin` 即恢复本体原行为。验证：`build_modding_guard2_20260913.log` BUILD SUCCESSFUL；`server_smoke_modding_guard2_20260913.log` `Done (1.943s)!` 无新增错误；实机清单见 `docs/plan/RVP有人载具潜行右键改装屏蔽方案_20260913.md` §4.1 |


| 2026-09-14 | **挂架"自动追踪首次状态"诊断改由调试命令控制**：`RVP_CustomMountRenderLogic.autoTraceRuntimeState` 首行增加 `DEBUG_ENABLED` 前置短路（原为无条件执行，每帧每车都要付"当前武器解析 → resolveMounts 全量解析 + `getWeaponId().toString()` + 拼 traceKey + `ConcurrentHashMap.putIfAbsent`"） | **降级**：不再"默认自动捕获"——复现挂架问题时须先执行 `/rvpdebug custommount on`（该命令会先 `clearDebugLog()` 清掉 `AUTO_TRACE_KEYS`，随后每个「载具实体+载具类型+当前武器」组合首次出现时仍记录一次，**能力本身未删**） | 仅自定义挂架的渲染诊断；挂架渲染 / 出弹点 / 弹药显隐等运行时行为零变化 | 恢复常开：删除该前置 `if (!DEBUG_ENABLED.get()) return;` 即可（代价是每帧每车重新付出解析与拼串开销）。依据：`docs/plan/RVP客户端掉帧性能审查_20260914.md` §2.2、§7.2。**验证**：`./gradlew compileJava` → `BUILD SUCCESSFUL in 1m 19s`；`build` 跑至 `:test` 时 93 个测试类全部 `failures="0" errors="0"`；产物 jar 已重出。命令用法：`/rvpdebug custommount on\|off\|status\|dump\|clear\|bolts` |
| 2026-09-15 | **弹道导弹尾迹 HBM 风格移植（广播尾迹收窄）**：新增 `RVP_RocketFlameParticle`（HBM ParticleRocketFlame/SmokePlume 移植，TRAIL/WASH 双模式）+ `missile_native_trail_particle_style=rvp_rocket_flame` 风格 + `missile_native_trail_ground_wash` 字段 + 双端桥 2 方法；**行为变更**：`broadcastTrailParticles` 对配置了非空 `missile_native_trail_particle_style` 的导弹整体跳过（服务端广播烟 + 燃烧期硬编码 FLAME 不再发送），避免与客户端本地定制尾迹叠加成"本地粗烟+广播细烟"两路混合 | **降级**：配置 style 的导弹不再有服务端广播尾迹（当前仅 9M723 配 `rvp_rocket_flame`）；无 style 配置的弹广播行为不变 | 仅"配置了 style 的导弹"；9M723 由 `trajectory_particle=none` + 本地 HBM 尾焰/烟浪全面接管 | 回退：删除 `broadcastTrailParticles` 开头的 style 跳过判据即恢复广播；9M723 JSON style 改回 `rvp_smoke` 即回灰烟观感。详见 `docs/plan/RVP弹道导弹尾迹HBM风格移植方案_20260915.md` |
| 2026-09-15 | **雷达手动锁自弹降权**：`RVP_RadarRoleHelper.collectManualLockCandidates` 对自己载具发射的弹体（`RVP_BaseBullet.getShooterVehicle() == 本载具`）施加 +1000 评分罚分（高于方位×4+距离×0.01 的任意原始评分），自弹沉到手动锁候选末位——拦截作业时不再反复锁到自弹。参照箔条抗性罚分先例，**降权非禁锁**（循环到末位仍可选中）；枪手 AI 目标选择（本体 GunnerBrain）未动 | 有意变更：自弹从"常排第一"变为"排末位"；敌方/友军弹体、其余锁定期规则不变 | 仅玩家手动雷达锁选择（TWS 循环/自动首选）；枪手 AI 选目标不受影响 | 回退：删除该罚分分支即恢复原排序。依赖客户端 `shooterVehicle` 由 `resolveRemoteRefs` 从远程数据还原——弹体发射后首个远程数据包到达前有数 tick 无法识别自属 |
| 2026-09-16 | **分角度 RCS 雷达隐身 + gunner 索敌适配**（借鉴 MCH-Reforged front/side/rear 三档插值模型）：载具 JSON 新增 `rvp_radar_rcs_factor: [front,side,rear]` 与弹舱条目 `open_radar_rcs_multiplier`（逐弹舱开启增幅连乘），消费链三条：客户端雷达探测表后过滤（mechanical/phase 统一，豁免锁定目标与 RVP 弹体）、ARH 主动雷达导引头获取距离缩放（scanRadarTarget，AIR 主动红外不受影响）、gunner 索敌感知距离缩放（passesAspectPerception，仅索敌收集、跟踪保持不查）；ARH 相位改四段（原定开机距离起箔条判定+MSL 告警、TWS 中继保留，隐身缩减后的真开机距离才进自主段）| 有意变更：正面隐身载具的雷达发现/gunner 索敌距离按因子缩减；出烧穿距离锁定持续（探测难跟踪易）；gunner AI 目标选择不再无条件看见隐身载具 | 配置了 rvp_radar_rcs_factor 的载具；未配字段载具、本体 MissileEntity、AIR 弹、RADAR_SEARCH 告警行为不变 | 回退：移除三处调用即恢复。详见 `docs/plan/RVP_分角度RCS雷达隐身_20260916.md` |
| 2026-09-15 | **发射架部署状态机跨线程 CME 崩溃修复**（crash-2026-09-15_20.44.51-server，实机 `LauncherDeployStateMachine.tick:67 HashMap.computeIfAbsent` 抛 ConcurrentModificationException）：状态机类注释本就声明"服务端与客户端各跑一份"，但 `STATES`/`TRANSITION_LOG_THROTTLE` 是静态普通 HashMap 双端共用——单机下 `onServerTick`（服务端线程）与 `onClientTick`（客户端主线程）并发 tick，双线程同时 computeIfAbsent/put 即 CME（B6 时代遗留的潜伏竞态，非本轮组网改动引入；实机多发发射架车运作提高碰撞概率后暴露）。**修复**：状态表/节流表按端物理分离（SERVER_*/CLIENT_*，以 `vehicle.level().isClientSide()` 选表）；`LauncherDeployRuntimeManager`（ConcurrentHashMap，不崩但双端同键互相覆盖快照）一并按端分桶；`clear`/`put`/`get`/`clearVehicle` 加端参数，3 处调用点（EntityLeaveLevelEvent、tickCombat 无关的 WeaponBase 射击门控、内部）适配| **修复**：崩溃消除；副作用为双端快照不再互写（语义更正确，门控/姿态读取各自消费本端状态机输出） | 发射架部署载具（9k720 等带 deploy 配置的载具）| 回退：合并双端表即恢复（不建议，竞态仍在） |
| 2026-09-15 | **gunner 组网智能拦截 + 步行玩家攻击规则**：①新增 `RVP_GunnerEngagementNet`（静态 side-table：(维度,faction,目标UUID)→最后交战tick），`GunnerBrain.tickCombat` 发射后记账，`findBestTarget` 可拦截导弹层两池降权——同 faction 网络内窗口内已交战导弹沉入第二池、fresh 池空时忽略降权（降权非禁选）；②`isProtectedCreativePlayer` 创造模式无条件保护（移除困难难度例外）；③enemy.json target_types 补 `"player"`（原缺口导致 enemy gunner 从不打步行玩家）。新档案字段 `engagement_net_cooldown_tick`（默认 100，钳 [0,1200]，/reload 生效）| **行为变更**：困难难度创造玩家从可被打→保护；enemy 档案开始攻击生存步行玩家；同 faction 多 gunner 面对一波来袭从"全体锁同一枚"变为"弹幕分配"；载具/玩家层的集火行为不变 | 同 faction gunner 拦截 + enemy 步行玩家攻击；单人、跨 faction、载具集火不受影响 | 回退：删除两池过滤分支即恢复全局最近；isProtectedCreativePlayer 恢复难度例外；enemy.json 移除 "player"。详见 `docs/RVP_gunner/RVP_gunner组网智能拦截_20260915.md` |
| 2026-09-17 | **激光致盲系统（laser_data blind_* 三字段，默认关闭）**：`rvp:laser` 命中玩家/gunner/其骑乘载具时累计受击次数，`blind_hit_count` 次（`blind_hit_window_tick` 窗口内）触发致盲——**触发时计数清零、窗口重开、重新累计**，再次攒满刷新时长。玩家：服务端发新包 `S2CLaserBlind` → 客户端 `RVP_ClientLaserBlindState` + `RVP_LaserBlindOverlay` 白色闪光滤镜（前 70% 全亮后 30% 渐隐，登出清理）；gunner：`RVP_LaserBlindService` 致盲状态表（`isBlinded`），消费点 `GunnerWeaponSuitability.canSelectForTarget` 致盲分支——被致盲 gunner 仅雷达制导/雷达中段（SARH/ARH/AIR）导弹可选且要求 `hasUsableRadar`（96L6 搜索中继不算、TADS 算），其余武器不可选 → 索敌层自然失去目标；CIWS 不受影响。**`blind_hit_count` 默认 0 = 功能默认关闭**，未配置激光零行为变化；协议版本 11→12 | **有意变更（仅显式配置后）**：被致盲玩家获得白色闪光滤镜、被致盲 gunner 视觉索敌失效且武器受限；默认配置零变化；协议版本升级（旧客户端无法连新服，需同步更新） | 配置 blind_* 的激光武器与其目标 | 回退：不配置 blind_* 字段即无致盲（默认关闭），代码回退删 RVP_LaserBlindService/S2CLaserBlind/overlay 与两处调用点。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §十一 |
| 2026-09-17 | **弹药分角度雷达信号（ammo_radar_rcs_factor 全量取代 signal_intensity_factor_on_radar）**：导弹/炸弹/火箭弹新增 `misc_data.ammo_radar_rcs_factor: [迎头,侧向,尾向]`（与战机 rvp_radar_rcs_factor 同款 sin³ 曲线，参照方向=弹体速度方向，字段名含 ammo 防混淆），雷达探测弹药距程 = maxScan × 方向因子；①`RVP_MiscData` 删旧字段/resolver/默认工厂、新增三档（缺省 null→[1,1,1]，单档钳 [0.01,10]，可 >1 增透）；②`RVP_BaseBullet` 三档字段经生成包同步，`getSignatureSize()` 改返回侧向值（红外虚拟箱不变），新增 `getRadarSignatureTowards`（速度方向插值，零速度回退侧向）；③消费链：`RVP_RadarScanHelper.appendRvpAmmoTargets`（玩家客户端雷达+服务端 gunner 雷达表）与 `RVP_ExternalRadarSyncService.appendAmmoTargets`（顺带修复中继 sig 只当开关的不一致）改调方向因子；`GunnerTargeting` 弹药层（CIWS/近距/findBestTarget rvpAmmo 层）新增拦截感知门（索敌半径×因子，>1 由索敌 AABB 封顶）；④迁移脚本 `scripts/migrate_ammo_radar_rcs_20260917.py`（带 type=missile/bomb/rocket 断言，机炮跳过）：14 武器×4 副本——j16_akf98a/rafale_storm_shadow→[0.08,0.35,0.18]（j20a 同款），其余 12→[1,1,1]；主包+server 14/14、client_1/2 各 11（陈旧副本本就缺 3 文件），旧字段 0 残留；⑤新增 JUnit `RVP_AmmoRadarRcsTest`（迎头/尾向/135°侧向/单调性/零速度回退/全 1 恒等）。**平衡变化（用户定版）**：原 5/6 倍增透取消，改 1,1,1 按雷达标称距离探测；AKF98A/风暴阴影获得 j20a 同款迎头隐身 | **有意变更**：①原配 5/6 的 12 个武器弹药探测距离从 5-6×雷达标称缩回 1×（隐身弹突防窗口变大，属定版平衡变化）；②AKF98A/风暴阴影获得分角度隐身；③gunner 拦截索敌对 <1 因子弹药收缩；④红外弹对导弹锁定、机炮弹、载具探测链不变；⑤距离分区间能力废弃（实战只用过 [0,inf]） | 全部 RVP 导弹/炸弹/火箭弹的雷达探测与 gunner 拦截索敌 | 回退：`scripts/migrate_ammo_radar_rcs_20260917.py` 反向重写回旧字段 + RVP_MiscData/RVP_WeaponData/RVP_BaseBullet 恢复旧字段链。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §九 |
| 2026-09-16 | **搜索中继指示目标补保护判定（修复"创造+和平驾驶被攻击"回归）**：上一条搜索中继改造引入的 `GunnerBrain.tickTargeting` 指示回退（`getRelaySearchContact`）未过创造保护——搜索中继接触链 `findRelayScanTarget` 沿用"锁定仅是告警"旧语义刻意不做创造过滤，指示目标被采纳为 trackedTarget 后经 `maintainLocalLock` 本车落锁获得发射授权，绕过方案A保护。修复：`GunnerTargeting` 新增 `isValidDesignationTarget`（委托索敌同款 `isValidTarget` 全链：创造保护方案A矩阵/target_types/敌我），指示采纳前强制校验；基线 tickTargeting 断言同步追加。另：S400（96L6）告警点亮条件核对表落入方案文档 §八（96L6 存活/3500×因子/离地≥scan_min_height 25/60t 周期闪烁，低空只剩 BUK 告警属数据语义不对称，未改动） | **修复回归**：创造+非困难（和平/简单/普通）驾驶载具重新受保护（不被指示→不被落锁→不被攻击）；生存/创造+困难行为不变；S400 搜索告警（RWR 被动照亮）不属"攻击"，保持对所有模式发射 | 全部经搜索中继指示链的 gunner 交战 | 回退：tickTargeting 去掉 isValidDesignationTarget 校验即恢复（不建议，即恢复保护旁路）。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §八 |
| 2026-09-16 | **搜索中继/火控中继分离（96L6 vs IRIST TADS，"搜索中继不能锁定开火"语义定版）**：①配置：96l6.json 雷达 `radar_role` "fire_control"→"search"（4 副本同步，载具包不进 git；原配置错误使 gunner 能用 96L6 落锁开火）；②`RVP_ExternalRadarLinkHelper` 新增 `getPreferredRelaySearchRadar`（search 优先/最大扫描兜底）；③`GunnerExternalRadarController` 双分支：火控中继（TADS，role!=search）保留 detect→箔条→落锁→外置授权原链路；搜索中继（96L6，无锁雷达）只 `findRelayScanTarget`（RCS effectiveRange 门控）+ `detect` 喂探测表（RVP_WarnRelayService 由此发 "S400" RADAR_SEARCH）+ 接触侧表（40t 新鲜度）+ `isWithinRelaySearchVolume` 烧穿距离校验，**绝不落锁/写授权表**；④`GunnerBrain.tickTargeting` 自身索敌无结果时回退取 `getRelaySearchContact` 作 trackedTarget（仅指示：engage 转炮口，无锁 prepareLaunchLock 失败不发射）；⑤`RVP_GunnerRadarActions.maintainLocalLock` 距离门改**烧穿发射门**：`maxScan × combinedFactor`，出烧穿保持 100t（5 秒）宽限再脱锁（镜像客户端链语义），非隐身目标因子 1.0 行为不变；⑥诊断 RELAY_SEARCH_ACQ/LOST、LOCK BURN_THROUGH；⑦基线 `RVP_GunnerBehaviorBaselineTest` externalRadar/radar/tickTargeting 断言随新结构改写 | **有意变更**：带 96L6 的 SAM 阵地对隐身目标 = 远距只搜索告警+炮口指示不发射；进入发射车烧穿距离（1500×因子）才落锁告警并开火；TADS 火控中继行为不变；非隐身目标行为不变；96L6 雷达同时失去玩家外置雷达联动中的可锁性（role=search 被 canLock 排除，符合"搜索雷达不可锁"语义） | 所有 deployable_uav 雷达中继（96l6/tads 等）+ 所有 requireLock RF 武器的发射授权 + gunner 炮口指示行为 | 回退：96l6.json role 改回 fire_control + GunnerExternalRadarController 删除搜索分支即恢复"中继必落锁"。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §八 |
| 2026-09-16 | **乘员随载具隐身修复（"gunner 老远探测到隐身战机"第三轮）**：`GunnerTargeting.collectTargetEntities` 不排除骑乘者——驾驶隐身战机时玩家 Player 实体作为独立候选进入索敌（target_types "player" 命中），而 `passesAspectPerception` 原对非载具实体直接放行、不吃分角度 RCS → AI 在全索敌半径（有 96L6 中继 max 3500 格）"感知到驾驶员"（normalizeTarget 再解析回战机锁定开火）；AI 驾驶员是 GunnerEntity 非 Player，反向无此洞 → "AI 看得见我、我看不见 AI"不对称。修复：`passesAspectPerception` 对骑乘候选按所乘载具 combinedFactor 判定（`entity.getVehicle()` 解析），步行玩家/弹药维持放行；行为基线同步冻结 | **有意变更**：生存模式下驾驶隐身载具的玩家不再绕过载具 RCS 感知门（乘员随载具隐身）；创造模式原被创造保护掩盖无感；非骑乘目标（步行玩家/弹药）行为不变 | 配置 rvp_radar_rcs_factor 载具的驾驶员；未配置载具/步行玩家/CIWS 弹药拦截链路不变 | 回退：passesAspectPerception 去掉 getVehicle() 骑乘解析即恢复原状（不推荐，即恢复隐身失效）。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §七 |
| 2026-09-16 | **gunner 创造保护最终定版（方案A）+ 创造步兵/驾驶区分**（覆盖同日上午"恢复困难例外"中间版，用户两次澄清后选定矩阵）：①`GunnerTargeting.isProtectedCreativePlayer`（仅步行玩家分支）＝创造/旁观**无条件保护、任何难度都不被打**（难度不参与）；②`GunnerTargeting.hasProtectedCreativePassenger`（载具分支）＝车上存在创造/旁观乘员保护整车但**仅非困难难度**——`getDifficulty() != Difficulty.HARD`，困难难度下创造驾驶员/乘员的载具可被正常攻击；生存乘员不提供保护。最终矩阵：创造步行永不挨打；创造驾驶困难被打/非困难保护；生存任何状态被打；旁观永不。③行为基线 `RVP_GunnerBehaviorBaselineTest` 同步冻结两分支语义 | **有意变更**：创造玩家从"任何难度都不被打（09-15）→困难可被打（同日中间版）"最终定为"**步行永不挨打、驾驶仅困难被咬**"；难度从步行分支移除、只留在载具分支；生存行为不变；gunnerlock 诊断日志默认关零行为影响 | 所有 gunner（enemy/team/friendly 等全档案）对玩家目标的索敌保护判定 | 回退：难度整体重参与则改回 isProtectedCreativePlayer 加难度分支并同步基线断言；依据 `docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §六方案A |
| 2026-09-16 | **gunner 本地锁俯仰射界门修复**：`RVP_GunnerRadarActions.maintainLocalLock` 原把雷达 `rot_info` 的 `x_rot`（`x_rot_min=0/x_rot_max=85`，本体仅用作碟面动画钳制）当 MC 俯仰硬门（`VectorUtil.vecToRot` 负=向上），导致**视轴上方一切仰角目标恒 GATED 清锁**——gunner 贴脸/掠顶永远无法本地锁定（无 RADAR_LOCK、`prepareLaunchLock` 失败不开火）；玩家手动锁走 `ClientRadarAction.LOCK` 不经过此门故不受影响（"玩家能锁、AI 锁不了"即此分叉）。修复：删除 `aimRot.x` 俯仰判定、保留方位 ±45° 扇区判定（对齐本体扫描/探测只查方位的语义），语义定版**无死区**；同时新增 `/rvpdebug gunnerlock on\|off\|status` 常驻诊断（`RVP_GunnerLockDebug`，专用日志 `logs/rvp_gunner_lock_debug.log`，记录本地锁/开火事务/RCS 感知门/中继锁各门控出口）| **有意变更**：gunner 本地雷达锁不再有俯仰限制（原俯仰门从未正确工作过，等效上视≈0°）；修复后贴脸/掠顶可被本地锁定。玩家侧锁行为不变；隐身（RCS）、扫描、ARH、组网、箔条链路均不触碰 | 所有"雷达挂炮塔 + rot_info x_rot_min=0"的 gunner 载具（bukm3/cssa5/ps1sm/96l6 等）本地锁对空能力；其余载具（x_rot_min 为负的）仅少一道从未正确生效的门 | 回退：恢复 maintainLocalLock 的 `aimRot.x` 两行判定即回到修复前行为（不推荐，即恢复 bug）。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` |
| 2026-09-16 | **创造模式困难难度例外恢复 + 低空对空弹可选性修复**（"只锁定不攻击"排查，详见同日方案文档 §六）：①`GunnerTargeting.isProtectedCreativePlayer` 恢复 09-15 之前的老语义——旁观恒保护，**创造仅当难度非困难才保护（创造+困难=可被正常攻击）**，覆盖 09-15 c31171d9 的"无条件保护"（当时记录为用户定版，09-16 用户实测创造开飞机被打不了、明确要求恢复例外；步行/驾驶载具走同一判定一并生效）；②`GunnerWeaponSuitability.isAirTarget` 对 FixedWing/RotaryWing 恒真——9M317MA 等无 `lock_altitude_range` 的导弹被判"仅对空弹"后，原 AGL>25 硬判据会把 ≤25 格低空掠飞的固定翼整个拒出索敌候选池（AI 索敌不选目标→不攻击，中继锁不受此限→"只锁不攻"）；③`RVP_GunnerLockDebug` 插桩补全：LOCK 通道补 NO_TARGET/NO_RADAR 静默出口、新增 AI 通道 NO_TARGET（profile/索敌半径/驾驶员模式/难度）、SENSE 通道候选拒绝首因分类（CREATIVE_*/PROFILE_TYPE/ALLIED/WEAPON_UNUSABLE(AGL)/PERCEPTION）| **有意变更**：①困难难度下创造玩家（含驾驶载具）重新可被 gunner 攻击；②固定翼/旋翼任何高度可被"仅对空弹"gunner 选中（此前 ≤25 格 AGL 不可选）；③创造+非困难与旁观保护不变，生存模式不变；④诊断日志默认关，零行为影响 | enemy/team 等 gunner 对创造玩家的交战；带仅对空弹的 gunner 对低空飞行器索敌 | 回退：①删去 isProtectedCreativePlayer 的难度判断即回无条件保护；②isAirTarget 去掉 FixedWing/RotaryWing 分支即回纯 AGL 判据。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §六 |

> 原则：先让服务端能启动，允许功能丢失，但**必须记录**，不能静默丢失。

## 5. 服务端复现流程（标准操作）

1. 构建 RVP jar（`ywzj_rvp` 目录 `gradlew build` 或 `gradlew reobfJarNamed`），
   产物在 `build/libs/reobf/`（`ywzj_rvp-1.20.1-<ver>-reobf.jar`）。
2. 部署：将 RVP 与本体 jar 放入 `D:\ywzj\Forge-test-server\mods\`。
   - 本体：`ywzj_vehicle-1.20.1-0.5.8.jar`（只读，不动）
   - RVP：本次调试产物（覆盖前备份旧 jar）
3. 启动：在 `D:\ywzj\Forge-test-server` 下运行
   `java @user_jvm_args.txt @libraries/net/minecraftforge/forge/1.20.1-47.4.13/win_args.txt nogui`
   （Java 21，离线可启动；Yggdrasil 超时是噪音）。
4. 观察：成功标志 `Done (Xs)!`；失败看 `crash-reports/` 与 `logs/latest.log`。

### 5.1 开发环境冒烟（gradlew runServer，标准操作，2026-09-02 定）

适用于本仓库 `run/server` 开发服的快速启动验证（改 JSON / 数据包 / Java 之后）：

1. 后台启动：`./gradlew runServer --console=plain > server_smoke_<主题>_<日期>.log 2>&1`。
2. **每 10 秒轮询一次日志即可，不要一次 sleep 很久**：日志出现 `Done (Xs)!` 即通过；
   通过后用户通常手动关服，**结论一律以日志为准**（进程退出码 / 谁关的进程不代表结果）。
3. **冒烟结束必须显式关闭服务端并验证（2026-09-20 增补）**：`TaskStop`/终止后台 shell
   只会杀掉外层 bash，**不会递归终止 Gradle fork 的服务端 JVM**（Windows 无进程组语义，
   `GradleWrapperMain` + `BootstrapLauncher` 会孤儿化继续运行——此前多轮冒烟残留属实）。
   标准收尾：
   ```bash
   # 1) 找残留（GradleWrapperMain / BootstrapLauncher / net.minecraft.server.main）
   "C:/Program Files/Java/jdk-21.0.11/bin/jps.exe" -l
   # 2) 显式终止（taskkill 需全路径，bash 内 //F //PID）
   "/c/Windows/System32/taskkill.exe" //F //PID <服务端PID> //PID <wrapperPID>
   # 3) 复验 jps 干净 + 端口 25565 已释放（否则下次冒烟绑定失败）
   ```
   （`tasklist | grep` 在该环境输出不可靠，进程核查一律用 jps。）
3. 日志含中文（UTF-8）：grep 需加 `-a`（否则文件被当二进制跳过）；程序化解析用 UTF-8 解码，
   不要按 GBK。
4. 历史遗留噪音基线（**不算新错误**）：`Not found model file` ×8、create/tacz 类加载 WARN ×4、
   `abramsx.structure - 副本.json` Invalid path 等；与基线对比只看**新增**。
5. 验证完结束进程，不留挂起的服务端。

## 6. 当前问题上下文（纯 Forge 服务端启动崩溃）

- 现象：纯 Forge 1.20.1 专用服务端，本体 + RVP 同时加载即崩；
  仅本体正常（`Done (5.307s)!` 已验证）。
- 崩溃签名：`Attempted to load class org/ywzj/vehicle/vehicle/LocalVehiclePlayer
  for invalid dist DEDICATED_SERVER`
  - 本体侧：`AllEntities.<clinit>:160`（`YwzjVehicle.<init>:38` 触发）
  - RVP 侧：`RVP_Entities.<clinit>:27`
- 根因（已确认）：RVP 公共数组 mixin 改脏本体类 → Forge 重算栈映射帧 →
  解析到本体公共方法里的 `@OnlyIn(CLIENT)` 类型 `LocalVehiclePlayer` → RuntimeDistCleaner 拒绝。
- 详细证据：`docs/纯Forge服务端启动崩溃排查.md`、
  `docs/修复方向/修复方案总结.md`、`docs/修复方向/进度交接_20260806.md`（最新交接）。

## 7. 经验教训（2026-08-06 沉淀）

1. **崩溃根因是"帧重算时解析 `@OnlyIn(CLIENT)` 类型"**，不是本体 bug。
   排查先看 `RuntimeDistCleaner` 拒绝的类，再反查谁改脏了它，禁止先怀疑本体。
2. **公共数组 mixin = 服务端毒药**：目标公共类只要含 `@OnlyIn(CLIENT)` 类型引用
   （哪怕在惰性死代码分支），被任何 mixin 改脏就炸。新写 mixin 前先扫目标类常量池。
3. **`@Accessor` 同样改脏目标类**——"只加接口不改方法"也不行，照样全类重算帧。
4. **修复分层**：先"让系统跑起来"（可逆配置改动）→ 再"逐项恢复功能"（每次记录）。
5. **`instanceof` 降级 vs 强转**：对扩展接口一律 `instanceof` 判断，禁止强转
   （mixin 缺失时强转抛 ClassCastException，instanceof 仅静默失效）。
6. **交接 > 聊天**：关键中间产物（中毒方法清单、对照表、命令、日志路径）必须落盘。
7. **用户已否决 `@Overwrite`**：恢复带毒类功能只能用非 mixin 路径
   （继承覆写 / 事件 / 网络包 / 数据层），不允许再提 mixin 重写方案。
8. **本体作者确认（2026-08-06）**：本体公共方法引用 LVP 是设计如此（客户端专用方法，
   如 `readSpawnData`，服务端不执行、dist cleaner 会剥离 `@OnlyIn` 方法）；责任仍在 RVP
   侧——① mixin 帧重算触发不该发生的解析；② RVP 自身公共代码引用客户端类型必须补标
   `@OnlyIn(CLIENT)`（发现 `RVP_ExternalRadarLinkHelper` 3 个方法未标，列为阶段 1.5）。
| 2026-09-17 | **同轴开火误驱动蓄力修复 + railgun 跌回时长 0.2 秒**：①`RVP_WeaponFireController.syncClientInput` 的 `isFireKeyDown` 回退分支（武器未被选中时任意开火键都算开火，为多管齐射设计）导致打同轴机枪时同武器站 railgun 的蓄力条/转管一起涨（CHARGE 同病）——修复：蓄力类模式（CHARGE/RAILGUN/MINIGUN）的积累只认本武器被选中（主/副操作位）时的开火输入（`chargeOwnInput` 门），蓄力中切走武器按松开处理快速衰减归零；CIWS 与服务端射击授权不变；②ztz100_dtc251/dtb251.json `charge_decay_tick` 10→4（跌回 0.2 秒，用户定版） | **有意变更**：蓄力类武器不再被同站其它武器的开火输入驱动（打同轴不再涨 railgun 蓄力）；railgun 松开跌回 10t→4t；其它武器（FULL_AUTO 等齐射语义）与多管齐射行为不变 | 共用武器站的 CHARGE/RAILGUN/MINIGUN 武器（ztz100 dtc/dtb251 railgun+同轴等） | 回退：去掉 chargeOwnInput 门即恢复旧行为；charge_decay_tick 改回 10。依据：同会话修复记录 |
| 2026-09-17 | **激光武器命中载具触发激光照射告警**：`RVP_LaserWeapon.shoot` 射线命中载具时调 `RVP_LaserWarnService.warnLaserHit`——敌对（Team 联盟判定同 scanLevel）才告警，向目标乘客发 `S2CMissileTrackAlert(TYPE_LASER)`（客户端 laser_alert 音效 + 被激光照射提示），同"射手车×目标车"对 10t 节流；`RVP_LaserWarnService` 新增公开入口与节流侧表。此前 rvp:laser 命中无任何告警（LWR 仅由 LBR/LH/SALH 操作手照射会话触发）。文档同步：《RVP 激光武器数据模型文档》联动表/伤害结算节 | **有意变更**：rvp:laser 命中敌对载具即触发被激光照射告警（此前无任何告警）；照射会话型 LWR、友方不告警、创造载具可收告警（告警非攻击）不变 | rvp:laser 与其宿主载具（炮手/玩家操作均适用） | 回退：删除 shoot() 内 warnLaserHit 调用即恢复。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md` §十 |
| 2026-09-17 | **蓄力条 HUD 定位修复（转动炮塔时 UI 乱跑）**：`RVP_ChargeBarOverlay` 原把蓄力条锚定在武器瞄准射线的世界落点（`weaponUnit.weaponHitPos` → `VectorUtil.worldToScreen` 投影 + 上一帧插值），转动炮塔时落点在世界中扫动 → 屏幕投影坐标随之变化 → 蓄力条在屏幕上乱跑；而 DIRCM/APS/ECM/过热/反制等辅助 HUD 均为固定屏幕坐标。修复：删除世界投影与插值段，定位改为固定屏幕坐标（水平居中、准星正下方 72px，与原瞄准命中时的视觉位置一致），条形绘制与颜色不变；同步清理 Mth/Vec3/VectorUtil 失效 import | **有意变更**：蓄力条从"跟随武器落点"改为固定屏幕准星下方（转动炮塔时不再移动）；CHARGE/MINIGUN/RAILGUN 三类蓄力武器生效；条形样式/百分比文案/蓄满逻辑不变 | 仅玩家在载具内操作蓄力类武器时的 HUD | 回退：恢复 RVP_ChargeBarOverlay 的 weaponHitPos 投影段即回旧行为。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md`（同会话修复记录） |
| 2026-09-17 | **蓄力松开后客户端快速衰减修复（蓄力条冻结在松开值）**：`RVP_WeaponFireController.syncClientInput` 中 CHARGE/RAILGUN 松开开火键只置停止标志、客户端 `chargeTick` 不再更新——蓄力条冻结在松开瞬间值，直到下次按下才归零重爬（服务端 tick 早有 `charge_decay_tick` 衰减，纯客户端显示断层）。修复：CHARGE/RAILGUN 松开后客户端按 `charge_decay_tick`（步长=cap/decayDuration）衰减到 0（镜像服务端 tick 语义），蓄力条跟随真实值平滑快速跌落；MINIGUN 原有衰减不变；再次按下仍归零重爬不受影响。同批：ztz100_laserweapon.json（4 副本）配 `charge_decay_tick: 10`（0.5 秒跌落动画） | **有意变更**：CHARGE/RAILGUN 松开后客户端蓄力读数快速归零（此前冻结）；`shouldAttemptClientShot`/服务端语义不变；MINIGUN 不变 | 全部 CHARGE/RAILGUN/MINIGUN 蓄力类武器的客户端蓄力显示 | 回退：删除 syncClientInput 两个衰减 else-if 分支即恢复冻结表现。依据：`docs/RVP_gunner/RVP_gunner本地锁俯仰门修复_20260916.md`（同会话修复记录） |
| 2026-09-19 | **BVR 弹药探测真根因修复（广播克隆 signatureSize=0 不可探测）**：radarammo 诊断日志实证——超视距弹药以广播克隆（serverEntities，dist 1600+ 格）存在但 `detectable=false`、进入 512 格转 local 后 `detectable=true`：克隆体无 initFromWeapon/生成包数据，`signatureSize` 保持字段默认 **0** → `isRadarDetectableAmmo()` 恒 false → `appendRvpAmmoTargets` 全跳过。修复：可探测判定补 `radarRcsSide > 0`（克隆 radarRcs* 字段默认 1.0 → 判真，探测半径 = maxScan × 1）；隐身弹配置微小因子的克隆按 1 处理（广播数据不携带分角度因子）。诊断工具：`/rvpdebug radarammo on|off`（`RVP_RadarAmmoDebug`，logs/rvp_radar_ammo_debug.log，记录 local/clone 计数、每弹 dist/sig/pass） | **修复**：BVR 弹药探测恢复（克隆进探测表，半径 = maxScan × 1）；512 内行为不变；克隆统一按标称 RCS（分角度隐身因子仅对本地同步实体生效）；MACHINEGUN 类仍不可雷达探测 | 全部 RVP 导弹/炸弹/火箭弹在玩家雷达上的超视距探测 | 回退：isRadarDetectableAmmo 去掉 radarRcsSide 分支。依据：同会话 radarammo 诊断日志实证 |
| 2026-09-19 | **同步边界鬼影修复 + 广播克隆继承分角度隐身因子**（radarammo 诊断确认后追加）：①交界处静止鬼影——导弹进入本地同步范围后，其广播克隆仍残留在 serverEntities 中且不再更新，雷达接触表保留这个冻结克隆（有速度矢量、无法锁定、静止于同步边界）；修复：`RVP_ClientRadarTickHandler.mergedClientEntities` 在本地实体已同步时从 serverEntities 移除同 id 克隆（本体探测表按 id 去重，local 自动取代克隆）。②隐身弹超视距可见——广播克隆 radarRcs* 恒字段默认 1（分角度因子不随广播 NBT 同步）→ 隐身弹 BVR 按标称 RCS 显示；修复：`RVP_BaseBullet.readData` 末尾按 remoteWeaponId 走 `getResolvedWeaponConfig()`（客户端配置索引，getWeaponId 回退 remoteWeaponId）继承 `ammo_radar_rcs_factor` 三档 + signatureSize=侧向值——隐身克隆 BVR 同样按分角度因子收缩探测半径。同批：`RVP_MiscData` 类注释封印已删参数 signal_intensity_factor_on_radar（JSON 写该键被 Gson 静默忽略，等效恒 1，勿恢复） | **修复**：①同步边界（~视距）不再残留静止鬼影接触；②隐身弹（akf98a/风暴阴影）超视距广播克隆按分角度因子收缩探测半径（迎头 0.08 → 仅 maxScan×0.08 内可见），非隐身弹（[1,1,1]）BVR 探测不受影响；③signal_intensity_factor_on_radar 键即使写在 JSON 也确认无效（封印） | bukm3/cssa5 等 phase 雷达的超视距弹药接触；隐身弹 BVR 突防观感 | 回退：①去掉 mergedClientEntities 的克隆移除行；②删 readData 的因子继承段。依据：同会话 radarammo 诊断日志 |
| 2026-09-19 | **中继雷达弹药探测半径 √maxScan 单位错误修复（bukm3+96L6 BVR 探测失效）**：`b9a74592` 给中继链 `RVP_ExternalRadarSyncService.appendAmmoTargets` 加分角度因子时写错单位——`effectiveMaxSqr = maxDistance × sig²`（线性值）却与 `distanceToSqr` 比较，实际探测半径 = **√maxScan × sig**（96L6 3500→59 格），BVR 探测塌缩到比视距还近；主链 `RVP_RadarScanHelper`（`maxScan² × sig²` ✓）与 GunnerTargeting 感知门（`radius × factor` ✓）写法正确，故飞机链正常、Buk 中继中招。修复：`maxDistance × maxDistance × sig × sig`（有效半径 = maxScan × 分角度因子）。另查明：**bukm3 自车雷达（1500）对弹药结构性封顶 512 格**——自车雷达为客户端链路，MC 服务端实体同步硬上限 = 视距 32 区块，视距外弹体实体不同步，与 RCS 改动无关（09-17 前的 ×5 倍率在该链同样无效）；BVR 感知正解 = 96L6 中继链。同批：凝结云尾迹粒子层间抖动降频（原每帧重掷为帧率级高频闪烁，改每 tick 重掷 + prev/current 按 partialTick 插值） | **修复**：①96L6/Buk 中继对弹药探测半径从 59 格恢复 3500 × 分角度因子；②尾迹抖动从帧率级降为 tick 级平滑。自车雷达 512 格结构顶为 MC 实体同步上限，未改动（可选后续：RemoteAmmoSyncService 6144 格快照并入自车雷达显示，需用户定版） | bukm3+96L6 组网及所有走中继快照链的弹药探测；凝结云尾迹观感 | 回退：恢复 `maxDistance × sig²` 即回 59 格塌缩（不建议）。依据：同会话排查 |
| 2026-09-19 | **同轴开火误驱动 railgun 蓄力回归修复（0917 修复的输入源遗漏）**：用户实测蓄力回归——真凶是 `RVP_WeaponFireController.isFireKeyDown` 用 `getCurrentWeapon() == weapon` **恒等比较**选输入键：Multi 组内的 dtc251 恒等比较恒假 → 落进"任意开火键都算"兜底 → 打同轴（副键）时 railgun 的 fireDown 也为 true；而 0917 的 `chargeOwnInput` 门在"railgun 正是当前主武器"时恰好放行。0917 只修了门、没修输入源。修复：`RVP_WeaponResolveHelper` 新增 `currentSecondary`（unwrap），`isFireKeyDown` 主/副选中判定全部改解包比较（副选中武器只听副键）。同日观瞄准星全局化配套：`RVP_CcipUtil.computeBulletImpact` march 增加飞行距离帽 512 格、无命中返回 null（准星居中，修天空垂落）；伪装过渡改 disguise 1t + blend 3t smoothstep 缓动 | **有意变更**：①代理包装武器（Multi/Agent 组内）的开火键归属按解包后选中判定——主选中只听主键、副选中只听副键，非选中武器维持任意键兜底（其余行为不变）；②伪装迁移 1tick 即开始、3tick smoothstep 完成；③观瞄准星对天空居中不垂落 | ztz100 railgun+同轴等共用武器站的蓄力类武器；全部观瞄 RVP 武器准星；配置伪装的载具伪装过渡 | 回退：isFireKeyDown 恢复恒等比较即回退（会重现蓄力回归，不建议）；march null 回退改弹道终点即回天空垂落（不建议）。依据：`docs/plan/RVP观瞄视角射弹原点分离方案_20260918.md` §六 |
| 2026-09-18 | **观瞄视角射弹原点分离（rvp_sight_fire_disguise，新功能）**：载具 JSON 武器站新增 `rvp_sight_fire_disguise{disguise_ticks=4,blend_ticks=3,max_distance=32}`——玩家处于观瞄视角（SCOPE）操作本站开火时，实际弹体从观瞄相机坐标射出（方向不变=准星方向，消除观瞄离炮闩枢轴的抵近偏差），客户端 disguise 期把模型/曳光/尾焰平移渲染为"从炮口射出的平行弹道"、blend 期线性合流真实弹道，炮口烟特效天然留在炮管。链路：客户端 SCOPE 状态差分+30t 心跳（新 C2S `C2SScopeViewSync`，协议 12→13）→ 服务端 `RVP_ScopeViewStateTable` → `RVP_ProjectileSpawner` 旧入口覆盖 muzzle + 伪装数据随生成包同步 → `RVP_SightFireDisguiseRender` 平移渲染；服务端/客户端尾迹在伪装期跳过。**参数在站层不在弹层**：观瞄是站属性，改装弹种每变体一文件易漏、武器 JSON 跨车共享会泄漏；改装弹种兼容性由覆盖点位置保证（服务端 spawn 层按射手状态判定，改装代理 `VehicleMultiWeapons.doClientShoot` 不经 RVP 子武器方法也不受影响）。ztz100 `turret` 站已配试点（该文件仅本地主包有，run 副本无 ztz100.json） | **有意变更（仅显式配置后）**：观瞄视角下实际弹道原点改为观瞄相机（弹着点=准星），视觉伪装数 tick；未配置载具/gunner/第三人称/座舱/未开镜零变化；协议 12→13 旧客户端不能连新服（同 0917 惯例） | 配置站的全部弹种（含改装变体）在观瞄视角下的射击 | 回退：删载具 JSON 配置块即完全关闭；代码回退见方案文档 §五。依据：`docs/plan/RVP观瞄视角射弹原点分离方案_20260918.md` |
| 2026-09-18 | **热量表双线程 CME 崩溃修复 + gameTime 时钟**（crash-2026-09-18_05.10.20-server，单机 integrated server 服务端线程 tick 载具时 `RVP_WeaponHeatManager.weaponHeatState` HashMap.computeIfAbsent 抛 ConcurrentModificationException）：热量跨端共享表（09-17 未提交 WIP）为普通 static WeakHashMap/HashMap，单机下**服务端线程**（载具武器 tick）与**客户端线程**（客户端武器副本 tick + 热 HUD 每帧读取 + 激光过热渲染门 canRenderBeam）并发 computeIfAbsent/扩容同一张表 → CME；与 2026-09-15 LauncherDeployStateMachine CME 同类，但本表设计目的就是跨端共享条目（客户端 HUD 读服务端热量）**不能按端拆表**，改加静态 `LOCK` 互斥锁覆盖全部 public 入口（tick/canShoot/onShotFired/currentHeat/maxHeat/heatRatio/hasHeat/isOverheated），锁内同时保证 HeatState 读改写原子。**顺带修掉锁解决不了的第二颗雷**：tickState 原用实体 `tickCount` 作冷却时钟，客户端/服务端实体 tickCount 是独立计数域，交织写 lastTick 后另一端算出巨大 elapsed 把热量瞬间清零（过热永远攒不起来）；时钟改双端同域的世界 `level().getGameTime()`（服务端同步到客户端，±1t 抖动无感知），HeatState.lastTick 改 long 防老存档溢出 | **修复**：崩溃消除；单机过热可正常攒满/冷却（此前即使不崩，热量也会被双端时钟冲突瞬间清零）。共享语义不变（客户端 HUD/激光过热门仍读服务端热量）；专用服各 JVM 一张表不变；行为面零变化 | 单机（integrated server）全部配热量的 RVP 武器（heat HUD、过热门、激光过热熄束） | 回退：去掉 LOCK 与 gameTime 时钟即回到崩溃/热量清零状态（不建议）。依据：同会话崩溃分析 |
| 2026-09-18 | **烟雾弹反制激光压制（激光射线命中烟幕 AABB 截断）**：`RVP_LaserRaycast.traceFrom` 在方块/实体命中解析后新增第三步烟幕截断——射线与存活 `RVP_SmokeEntity`（rvp:rvp_smoke）AABB 求交（`getEntitiesOfClass` 射线走廊盒 + `AABB.clip` 取最近入射点），命中即提前判命中并截断：光束止于云团表面（SURFACE_INSET 内缩）、`hitEntity=null`/`blockHit=null` → 服务端伤害/TYPE_LASER 告警/致盲随实体命中门控自动跳过、命中火花不生成，弹药与热量照常消耗。`RVP_LaserBeam` record 新增 `smokeBlocked` 字段（`RVP_LaserBeamSmoothing` 构造同步）。不分敌我；飞行段小箱与爆炸后云团箱统一判据；双端共享同一射线故服务端结算与客户端渲染截断点一致；gunner 激光同样被反制。发射端已在烟幕 AABB 内部时不截断（原版 clip 只求外部入射点） | **有意变更**：烟幕成型后（及飞行段小概率）敌我双方激光射入烟幕 AABB 即被吸收，目标不再受伤害/告警/致盲；无烟幕时激光行为零变化；SALH/LBR/LH 激光制导链、目标指示吊舱标记射线、本体 SmokeGrenadeEntity 不在截断范围 | 全部 rvp:laser 武器（玩家与 gunner 操作）vs 配置 countermeasure.smoke 的载具（ztz100/t90m/m1a2sep/vt4/mi28 及全部飞机） | 回退：删除 traceFrom 烟幕截断步与 `RVP_LaserBeam.smokeBlocked` 字段（含 smoothing 构造参数）即恢复激光穿透烟幕。依据：`docs/plan/RVP武器数据模型/RVP 激光武器数据模型文档.md` §射线检测 |
| 2026-09-18 | **激光武器渲染/伤害分叉与命中点偏移修复（双 bug）**：①过热仍渲染——`RVP_LaserWeapons.canRenderBeam` 原只查换弹/弹药，服务端 `shoot()` 过热门（`canShootHeat`）拒绝伤害后客户端 `RVP_ClientLaserDriver` 按住开火键仍每 tick 续脉冲，光束照常渲染；修复：`canRenderBeam` 追加 `!RVP_WeaponHeatManager.isOverheated(laser)`（与 `canShoot` 严格互补；不查 `isCoolingDown()`，shoot_interval 逐发冷却不剪持续光束的设计保留）。②命中生物光束折向脚底——原版 `ProjectileUtil.getEntityHitResult` 返回的 `EntityHitResult` 位置= `entity.position()`（脚底），`RVP_LaserRaycast.traceFrom` 直接用作光束终点与伤害段终点；修复：对命中实体碰撞箱 `getBoundingBox().clip(start, hitEnd)` 重求真实入射点（0 膨胀与 ProjectileUtil 内部一致，clip 失败回退 `entity.position()`），渲染端点/冲击特效/服务端 `resolveHitboxDamage` 段终点三处一并修正 | **有意变更（均为修正）**：①过热瞬间光束立即熄灭、冷却降过满额恢复（此前过热中光束持续渲染但无伤害）；②光束终点=命中实体碰撞箱真实入射点（此前=实体脚底）；③载具命中箱因子改按真实命中部位结算（此前射线段终点为载具 position 脚底点，部位解析偏下）；未配热量的激光武器（max_heat_count<=0）行为不变 | 全部 rvp:laser 武器的客户端光束渲染与服务端命中结算 | 回退：①删 canRenderBeam 的过热判据；②traceFrom 恢复用 `entityHit.getLocation()`。依据：`docs/plan/RVP武器数据模型/RVP 激光武器数据模型文档.md` §射线检测/§弹药过热与射击门控 |
| 2026-09-19 | **弹体尾焰隔烟全亮可见修复（entityTranslucent 延迟 flush 绘制顺序问题）**：实机症状"透过浓浓地面烟浪仍能看到导弹尾焰、且不像经过半透明处理"。根因不在粒子深度测试——`RVP_BedrockProjectileEntityRenderer.renderMotorFlame` 把尾焰网格以 `RenderType.entityTranslucent` 写进实体渲染**主 bufferSource**，该类顶点被推迟到粒子阶段**之后**的 translucent 统一 flush，绘制顺序=地形→实体→粒子烟浪→尾焰，任何粒子烟都画在尾焰之前、无法半透明衰减它（粒子烟 depthMask(false) 只能靠后画混合衰减，深度帮不上）。修复：新建独立 `FLAME_IMMEDIATE_SOURCE`（`MultiBufferSource.immediate`，仅渲染线程）承载尾焰顶点并**当场 endBatch**——尾焰在实体阶段内立即上屏（先于一切粒子），后画的烟浪正常叠上来半透明衰减；entityTranslucent 写深度，尾焰后方的烟仍被深度剔除，前后遮挡语义恢复。本体 `RocketPropelledEntityRenderer` 用 `RenderType.eyes`（加亮不写深度）同样后置 flush，但加亮观感下"隔烟透光"不违和，未动 | **有意变更**：RVP 弹体尾焰从"最后绘制（盖过所有粒子烟）"改为"实体阶段绘制（可被前景粒子烟半透明衰减）"；尾焰与弹体本体网格的重叠处改由本体覆盖（原为尾焰覆盖，喷口重叠区极小）；本体弹（eyes 加亮款）与其余渲染路径零变化 | 全部配 `has_rocket_engine` 的 RVP 弹体尾焰观感（发射段穿烟浪场景最明显） | 回退：renderMotorFlame 恢复写主 bufferSource 且去掉 endBatch 即回旧行为。依据：同会话实机反馈与 MC1.20.1 渲染阶段分析 |
| 2026-09-19 | **RVP 粒子接入本体热成像白热通道（火箭尾迹烟/MCHR 爆炸烟/视觉工厂粒子热成像下发白）**：本体 `ThermalHandler` 的白热机制是"画进 PostChain `thermal_buffer` 者即热源"——AFTER_ENTITIES 重画实体、AFTER_PARTICLES 只重画本体 `SmokeCloudParticle`（经本体 mixin accessor 取批次）、AFTER_LEVEL 由 thermal.fsh 按 `heat=0.4+0.6×luma` 混合白热；**未画进 buffer 的粒子一律冷背景**，RVP 自定义粒子全部不发光。修复（零 Mixin）：新建 `RVP_ThermalParticleChannel`——四个 RVP 粒子类（RocketFlame/MchrSmoke/MchrFlare/WhitePhosphorus）构造器向弱引用登记表自登记（引擎持强引用，移除后弱条目随 GC 回收）；`AFTER_WEATHER`（本体烟通道之后、后处理之前）反射读本体私有 `thermalChain`（mod 类字段名不混淆，失败一次告警后静默停用）取 `thermal_buffer`，把登记粒子按各自 RenderType 分组重画进去；渲染状态镜像本体烟通道（粒子 shader/光照层/depthMask(false)/fabulous 还原 particlesTarget）；RVP 电视导引 THERMAL 模式同样走 ThermalHandler.setActive，一条通道覆盖观瞄+HITL 两种热成像 | **有意变更（仅热成像激活时）**：RVP 火箭尾迹烟/发射烟浪、MCHR 爆炸烟（视觉工厂烟体）、曳光火花、白磷烟在白热画面中作为热源发白（亮度=颜色 luma 折算：灰白烟 0.73~0.94、橙焰≈0.81，烟焰有层次）；关闭热成像零开销零变化；反射失败仅 RVP 粒子不发白，本体热成像不受影响 | 全部 RVP 自定义烟雾/火光粒子的热成像可见性；服务端零变化 | 回退：删四个粒子构造器的 register 行即完全关闭（通道空表零开销）；删通道类亦同。依据：本体 `ThermalHandler`/`thermal.fsh` 源码分析（同会话） |
| 2026-09-19 | **ERA 爆炸特效升级为 MCHR 爆炸视觉（用户定版：烟雾规模对齐武器爆炸半径 3）**：两条 ERA 失效爆点（直击 `tryDestroyBoneModules` 命中点、爆炸波及 `destroyModulesByExplosionRadius`→`spawnEraEffect`）原为原版 `EXPLOSION`+`SMOKE` 粒子 + 服务端 `GENERIC_EXPLODE` 音，升级为统一发布 `RVP_DefaultExplosionVisualService.spawn`（`rvp:mchr_explosion` 事件）：半径 = 3 × 模块 `explosion` 缩放（未配置即 3，与武器爆炸半径 3 的烟雾同规模）、MISSILE 战斗部音色、音效由事件客户端按声速延迟播近/远音（服务端不再补播 GENERIC_EXPLODE 避免双重音）；**爆炸波及路径特效位置从车体包围盒中心改为失效骨块 OBB 世界系中心**（复用 `resolveBoneObbs`，结构模型不可解析回退车中心；直击路径维持命中点优先）——多块连爆不再在同一车心叠加 | **有意变更**：ERA 直击/爆炸波及失效的表现从原版小烟团升级为 MCHR 风格爆炸（闪光+灰黄翻滚烟+碎屑，radius 3 规模 ≈26 团烟）；爆炸波及多块时各在自己骨块位置爆；音效从服务端即播改为声速延迟近/远音 | 全部配 `bone_modules`/`hitbox_era` 的载具 ERA 失效表现；模块判定/伤害/状态同步链路零变化 | 回退：两处调用恢复原 sendParticles+playSound 段即回旧表现。依据：同会话用户需求（MCHR 烟雾规模=武器爆炸3） |
| 2026-09-19 | **热成像粒子通道错位修复（AFTER_WEATHER→AFTER_PARTICLES，双重视图根因）**：实机症状——热成像下爆炸烟两份：爆点一份暗烟（主画面正常渲染、未进热缓冲呈冷背景）+ 偏离爆点的方位一份白烟（thermal_buffer 染色副本错位）。javap 反汇编 Forge 1.20.1 `LevelRenderer.renderLevel` 定位：vanilla 在**粒子渲染之后**（bytecode 2761-2778）才把视图矩阵 push 到 `RenderSystem.getModelViewStack()`（云/天气/世界边缘用世界坐标渲染需要），尾部 3028-3033 才 pop——故 `AFTER_WEATHER` 阶段 ModelViewStack **已含视图矩阵**，通道再 `mulPoseMatrix(eventPose)` 即**双重视图变换**，粒子方位被转过头；而 `AFTER_PARTICLES` 阶段 ModelViewStack 仍为 identity，自行补视图恰为单次变换（本体 ThermalHandler 选此阶段的原因）。修复：通道事件阶段改 `AFTER_PARTICLES`（与本体烟通道同阶段，各自独立 listener 无冲突），渲染数学不变 | **修复**：热成像下 RVP 粒子白热副本回到爆点正确方位（与主画面烟重合叠加）；此前错位不复存在；其余行为零变化 | RVP_ThermalParticleChannel 全部场景（观瞄/HITL 热成像） | 回退：改回 AFTER_WEATHER 即重现错位（不建议）。依据：javap 反汇编 LevelRenderer（本会话，D:/ywzj/ywzj/_tmp_forge_src/levelrenderer_dump.txt） |
| 2026-09-20 | **flare.png 透明通道补全（热成像下 MCHR 爆炸火花"白底"修复，用户判断"透明通道没做好"属实）**：`textures/nuclear/flare.png` 原为 **mode=P 调色板图（连 alpha 通道都没有，等效全不透明）**、光斑渐变画在 RGB（中心白→边缘黑）——加色混合专用设计，主画面 additive（SRC_ALPHA,ONE）下"黑加 0"无底故平时正常；热成像通道把它重画进 thermal_buffer 时 additive alpha 方程把整张 quad 缓冲 alpha 写成 1，本体 thermal.fsh 以 alpha 为白热显形因子、判热阈值极低 → 整张 quad 显形、RGB 黑边折算 heat≈0.4 灰底板。修复：`scripts/fix_flare_alpha_20260920.py`（幂等可重跑）补 `alpha=luma(RGB)`（与 fsh 亮度折算一致），备份 `scripts/texture_backup_20260920/flare.png`（不放 resources 原处——processResources 无 exclude，.bak 会被打进 jar）；同族 smoke.png/particle_base.png 程序化检查均有真 alpha mask，不动；Java 零改动——SRC_ALPHA,ONE 保留（同时承担贴图调制与 vertex alpha 淡出，改 (ONE,ONE) 会使火花/闪光淡出失效） | **有意变更**：主画面 additive 光斑仅边缘略收紧（rgb×luma 平方衰减，火花/闪光生命极短基本不可感）；热成像下火花/闪光灰白底板消失、白热从中心按亮度自然衰减；核爆火光（同贴图消费点 RVP_NuclearVisualManager）同步受益 | flare.png 全部消费点（MCHR 火花/闪光、核爆火光）；smoke/particle_base 零变化 | 回退：`scripts/texture_backup_20260920/flare.png` 拷回 resources 即回旧贴图。依据：同会话实机截图与 thermal.fsh 源码分析 |
| 2026-09-20 | **热成像粒子提白 + effects_data 原版粒子（烟/火）接入热成像**（用户反馈：①粒子热成像下不够白；②effects_data 配置的原版粒子不变白）：①不够白根因——通道按各 RenderType 重画时 `SRC_ALPHA` 同时当 alpha 方程源因子，写入 thermal_buffer 的显形强度被平方衰减（MCHR 烟 vertex alpha 0.3~0.7 → 只剩 0.09~0.49；火箭烟每层再 ÷3）；修复：分组循环 begin 后统一覆盖混合/深度状态——`blendFuncSeparate(SRC_ALPHA, ONE, ONE, ONE_MINUS_SRC_ALPHA)`（RGB 亮度叠加、重叠烟快速饱和到白=热积累语义；alpha over 累积不衰减）+ `depthMask(false)`（MCHR 烟 RenderType 自带 depthMask(true)，写脏 thermal_buffer 的 copyDepth 深度会让后画粒子被前画剔除）；②原版粒子（trajectory_particle/missile_native_trail_particle 常配 CAMPFIRE_SIGNAL_SMOKE/smoke/flame 等）无法构造器自登记——**复用本体已注入的 `ParticleEngineAccessor`**（本体热成像同款途径，非 RVP 新增 Mixin）从引擎 translucent 批按白名单捞取：SmokeParticle/LargeSmokeParticle/CampfireSmokeParticle/BaseAshSmokeParticle/FlameParticle；本体 SmokeCloudParticle 跳过防本体烟通道双重提亮；accessor 缺失仅静默失去原版粒子路径 | **有意变更（仅热成像激活时）**：①RVP 全部烟/火粒子白热显著提亮（重叠区饱和到白）；②effects_data 配置的原版烟/火类粒子热成像下变白；非白名单原版粒子（传送门/音符等罕见类）与 opaque 批不受影响；热成像关闭零变化；服务端零变化 | 热成像视角全部粒子观感 | 回退：去掉 begin 后的覆盖段即回平方衰减亮度；删 collectVanillaThermalParticles 即回仅 RVP 粒子。依据：同会话用户反馈与混合方程分析 |
| 2026-09-20 | **温压蘑菇云（自绘几何特效）接入热成像白热**（用户反馈：温压爆炸蘑菇云热成像下不变白）：温压云**不是粒子**——`RVP_ThermobaricRenderer` 用 Tesselator 自绘四层几何（凝结云/压力波/尘环/烟云 alpha 混合 + additive 火球），不经粒子引擎，热成像通道两条粒子路径（登记表/引擎 translucent 批）均覆盖不到。修复：`RVP_ClientVisualEffect` 接口加 `default renderThermal(event)` 空实现（粒子类特效由通道粒子路径覆盖，空实现防双重提亮）；`RVP_ThermobaricEffectInstance` override 调 `RVP_ThermobaricRenderer.render`（渲染器**完全自包含**：pushPose+setIdentity+相机旋转的视图语义与进入时 ModelViewStack 无关——主渲染挂 AFTER_WEATHER、热成像窗口挂 AFTER_PARTICLES 语义一致；混合/深度/shader 自管理），热缓冲绑定时重调即写入：火球 additive→白热核心、云层 alpha 混合→白热显形、copyDepth 深度→被地形正确遮挡；`RVP_ClientVisualEffectDispatcher.renderThermal` 遍历活跃实例；`RVP_ThermalParticleChannel` 粒子画完后（热缓冲仍绑定）调用 | **有意变更（仅热成像激活时）**：温压弹四层几何热成像下白热显形；其它视觉工厂特效（默认爆炸 MCHR 粒子）走 default 空实现零变化；主画面温压观感零变化；已知边界：核爆视觉（RVP_NuclearVisualManager）同为自绘但走独立 S2C 非 dispatcher 体系，未接入（用户提及后可同法） | 温压特效的热成像可见性 | 回退：删通道里 renderThermal 调用即回冷背景（温压实例 override 留着无害）。依据：同会话用户反馈与温压渲染器源码分析 |
| 2026-09-20 | **内置 HBM 爆炸特效接入热成像白热**（同温压法）：内置 HBM 爆炸（`RVP_ExplosionVisualManager`，移植 HBM NTM Rebirth explosionSmall/Large 的独立 manager，非 dispatcher 体系）同为 Tesselator 自绘几何（灼热碎块/烟云 alpha 混合 + 冲击波 additive），热成像下原本呈冷背景。修复：manager 加公开 `renderThermal(event)`（EFFECTS 空早退，复用私有 render——渲染完全自包含：扩展投影矩阵 + setIdentity 相机旋转视图 + 收尾完整还原含 restoreProjectionMatrix，热缓冲绑定时重调即写入；深度测试沿用主画面拷贝深度，遮挡表现与主画面一致）；`RVP_ThermalParticleChannel` 在 dispatcher.renderThermal 后追加调用 | **有意变更（仅热成像激活时）**：内置 HBM 爆炸的烟云/灼热碎块白热显形、冲击波白热亮环，被地形正确遮挡；主画面 HBM 爆炸观感零变化；服务端零变化 | 内置 HBM 爆炸特效（rvp_hbm 类 effect_type 弹药爆炸）的热成像可见性 | 回退：删通道里 ExplosionVisualManager.renderThermal 调用即回冷背景。依据：同会话用户需求与 manager 源码分析 |
| 2026-09-20 | **RVP 弹体爆炸强制即时破坏路径（半径 >32 不再进本体核爆炸批处理，用户定版"不再被 32 截断"）**：本体 `VehicleExplosion` 的 `BATCHED_DESTRUCTION_RADIUS_THRESHOLD=32` 把 >32 爆炸切换到 `SphericalCollectionTask` 批处理（射线 8 倍、烧灼转化方块替换 2×setBlock+levelEvent 广播、主线程 20-30 tick 持续负载，实测 9M723 radius 64/33 卡顿来源）；新增 `VehicleExplosionImmediatePathMixin`（**@ModifyConstant**，`explode(Ljava/util/List;)V` 内 `32.0F` 常量，allow=2/require=1 覆盖路径分岔与 maximumRayEnergy 两处）+ `RVP_ExplosionImmediatePath` ThreadLocal 窗口（仿 RVP_TerrainOnlyExplosion）+ `RVP_Config.forceImmediateExplosionDestruction`（默认 true）；`RVP_BaseBullet.triggerExplosion` 经 `wrapImmediatePath` 把 A/B 两发爆炸都包进窗口——窗口内阈值抬到 Float.MAX_VALUE：恒走 ≤32 即时路径（GridCollectionTask + destroyBlocksImmediately 单 tick 完成），`maximumRayEnergy` 自动落回 radius×1.3 能量档（与 ≤32 爆炸同源公式，"同一路径套更高值"）；本体/其它 mod 爆炸不经窗口零影响。**Mixin 注入点说明（agents.md 纪律）**：@ModifyConstant 是改变该阈值分岔的最小注入；替代方案均不可行——本体无暴露阈值配置，VehicleExplosion 不可继承替换（伤害/视觉/掉落全依赖其 explode 管线），事件总线无路径选择事件；目标类双端安全（HurtSkip 已在公共数组注入同类） | **有意变更（仅 RVP 弹体爆炸）**：任意半径走即时路径——无烧灼方块替换（石→深板岩/沙→玻璃等不再发生）、无跨 tick 批处理负载，代价为破坏 tick 单次 hitch（半径越大 hitch 越长）；弹坑形状由 Grid 射线能量决定（与 ≤32 爆炸同源），与核爆路径的"核心+烧灼圈"形态不同；`crater_depth_rules` 在 Grid 路径经既有 `VehicleExplosionCraterMixin` 生效；配置 false 或本体爆炸恢复原行为 | 全部 RVP 弹体（machinegun/missile/rocket/bomb/dispenser）的 >32 爆破表现与服务端负载 | 回退：`forceImmediateExplosionDestruction=false` 即恢复本体阈值行为（mixin 保留无害）。依据：同会话用户需求（9M723 实测 33/64 卡顿，根因=Spherical 批处理+烧灼替换） |
| 2026-09-20 | **第一批四项（三代理调研后实施）**：①**爆炸伤害倍率拆分**：`hitbox_damage_factor` 经查对爆炸伤害完全不生效（`RVP_VehicleHurtScalingHandler:133` 有意豁免，爆炸原样透传）——新增载具参数 `explosion_damage_factor` / `explosion_damage_factor_default`（per-bone + 默认，`VehicleHitboxConfig`/`resolve` 参数化），`RVP_VehicleHurtScalingHandler` 爆炸分支乘算（爆心取 `DamageSource.getSourcePosition()`，倍率=爆心→载具中心线段命中的最近骨）；直击 `hitbox_damage_factor` 语义收窄为纯直击；②**本体爆炸视角晃动钳制**：client mixin `FirstPersonHandlerExplosionShakeMixin`（@ModifyArg `addExplosionShake` 强度 clamp ordinal 0 的输入值 × `explosionShakeIntensity` 0~1）——本体晃动唯一入口（爆炸 effect + 大口径导弹飞行 5t），RVP 声波屏震有独立配置；③**ERA 入射角 35° 上界前置**：直击命中激活 ERA 骨骼时伤害衰减角度钳 `min(实际, eraMaxIncidenceAngle)`（common 配置默认 35，90 关闭）；`hitboxRes` 计算上移到 incidenceMult 之前（无副作用顺序安全），跳弹判定用原始角不受影响，`lastImpactIncidenceAngleDeg` 字段不改写（调试/爆炸引信仍读原值）；④**载具物品副手静默拦截**：`RVP_OffhandVehicleSpawnGuard`（RightClickItem HIGHEST + OFF_HAND + instanceof VehicleSpawnItem → cancel，双端）——本体与 RVP 载具物品共用 VehicleSpawnItem 一类全覆盖，F 键误切副手后右键不再顺手放出载具 | **有意变更**：①爆炸伤害开始吃独立倍率（未配置不缩放，配了即生效——含本体武器爆炸）；②本体爆炸/导弹飞行晃动可被钳到 0；③ERA 骨骼直击的角度衰减按 ≤35°（默认）计；④副手载具物品右键被静默吞掉；RVP 声波屏震/主手/非载具物品零变化 | 载具爆炸伤害平衡、本体爆炸观感、ERA 直击平衡、载具物品操作体验 | 回退：①删载具 JSON 的 explosion_damage_factor 字段；②explosionShakeIntensity=1；③eraMaxIncidenceAngle=90；④删 RVP_OffhandVehicleSpawnGuard。依据：三代理调研报告（同会话） |
| 2026-09-20 | **第二批：爆炸倍率定位返工 + IR 离轴角四层修复 + 船适配 P1**：①**爆炸倍率返工**（用户澄清：explosion_damage_factor 应在武器侧管"弹药爆炸对载具"倍率，例：面板 300→偏差衰减 270→×2=540；载具侧已实现版保留但改名加 vehicle 前缀）——武器侧 `detonate_data.explosion_data.explosion_damage_factor`（默认 1.0）经 `RVP_ExplosionDamageFactor` ThreadLocal 窗口（triggerExplosion 包 A/B 两发）在 handler 爆炸分支乘算，**仅载具目标**（生物不乘，预乘 damage 会波及生物故否决）；载具侧改名 `vehicle_explosion_damage_factor(_default)`；结算=本体距离衰减 × 武器侧 × 载具侧（任一侧 ≠1 进入重放）；②**IR 离轴角 bug 修复（四层）**：bug A 实证=HMD 20t 保活保留超锥锁 + 发射授权只查锁存在（锥缘反复进出重置窗口→"偶尔能发射"）；bug B 实证=RF 落锁（90°锥）不查 IR 离轴角/EO 捕获只查窄 FOV/发射复核基准=伺服当前指向（空转）/服务端不查——修复：`RVP_IrLockHelper.isLaunchTargetWithinOffAxis`（boresight=中立安装轴或按 off_axis_stacks_with_station_rotation 配置、maxOffAxisLockAngle+容差）三点收口=客户端 passesShootLockGates 终检（超锥拒射提示 ui.ir_target_off_axis，新 lang 键）+ 服务端 dispatchShots 权威终检（防改包客户端）+ RF 供锁落锁前校验（锁建立即拦）；20t 保活保留（只管显示/锁维持，授权已解耦）；③**船适配 P1**：雷达早期 NCTR 标签（ScopeOverlay/TacticalMapScreen）加 VesselVehicle→"SHIP"；战术地图图标加 VesselVehicleData→ship.png（程序生成占位素材，正式素材可直接替换文件） | **有意变更**：①配了 explosion_damage_factor 的武器爆炸对载具伤害按倍率缩放（生物不乘）；②IR 离轴角外无法发射（含 EO/RF/切武器恢复全部来源），超锥拒射提示 ui.ir_target_off_axis；船在雷达/战术地图显示 SHIP 与船图标；未配 destroy_radius/explosion_damage_factor 的武器、直击倍率、本体武器爆炸对载具倍率（未配=1）行为不变 | IR 离轴角平衡、船载具体验、爆炸对载具伤害平衡 | 回退：①删武器 JSON explosion_damage_factor；②离轴角终检三处删除即回"锁存在即可射"；③④删 NCTR/图标分支。依据：三代理调研报告（同会话，bug A/B 逻辑链实证） |
| 2026-09-20 | **编译兼容修复：去除对本体 mixin 包的编译期依赖**（其它开发者拉取后 `RVP_ThermalParticleChannel.java:31` 编译失败：找不到 `org.ywzj.vehicle.mixin.client.ParticleEngineAccessor`）：该 import 是原版烟火粒子热成像读取引擎批次表的实现——本体的 mixin 包只存在于本体 dev 构建的 `-all` jar（`libs/` 本地依赖，未入库），其它开发者的本体发布 jar 编译 classpath 不含它。修复：`collectVanillaThermalParticles` 改运行时反射调用本体 accessor mixin 注入的 `getParticles()`（public、mod 自定义名不参与混淆、本体 ThermalHandler 强依赖故运行时必然存在），失败走既有 reflectionBroken 静默降级；**教训**：RVP 代码禁止 import 本体 `org.ywzj.vehicle.mixin.**` 包——那是我方环境才有的 dev 构件内容，跨环境编译必炸；需复用本体 mixin 注入产物时用运行时反射（mod 自定义名方法不混淆，安全） | **修复**：其它开发者的构建环境恢复可编译；本机与运行时行为零变化（反射调用与 cast 同一方法） | 仅编译期依赖面；运行时热成像功能不变 | 回退：恢复 import + cast 即回编译失败状态（不建议）。依据：其它开发者编译截图（同会话） |
| 2026-09-19 | **固体发动机凝结云保持期绑定燃尽 + 抖动按时间降频（用户需求三项）**：①凝结云保持期从"寿命前 45% 保持"定值改为**绑定发动机燃尽时刻**——`RVP_BaseBullet.ticksUntilMotorStopsBurning()`（max(一级燃尽, 二脉冲燃尽) - 当前 flightTick，钳 [0,1200] 防 motorBurnEndTick 默认 MAX_VALUE 撑爆）经桥 `addRocketFlameTrailParticle`（三处签名加 holdTicks）传给粒子，`holdEndTick = max(holdTicks+20, 108)`、`lifetime = holdEndTick + 120 + rand(60)`、α 在保持期恒 0.75 后 smoothstep 散开——长燃烧弹发射段云不再先于燃尽消散（"发动机开启时飞过的距离全程留云"）；橙焰相位改定长 24~36t（≈旧 0.12×240~340t 观感，火焰不随寿命拉长）；②③抖动重掷间隔从每 tick（20Hz）改为**随寿命 2t→14t 递增**（出生 10Hz 湍急→老期 1.4Hz 趋于稳定，模拟真实火箭发射湍流衰减）+ 渲染跨间隔 smoothstep 插值 + 幅度乘湍流因子 `1-0.65×smoothstep(ageRatio)`（年轻 1.0→老期 0.35；云散开 spread 保留，扩散照旧抖动趋稳） | **有意变更**：①固体款（rvp_rocket_flame，8 弹）凝结云保持期随实际燃烧时长伸缩（长燃烧弹更久、短燃烧弹保底 108t 不回退）；②全部 TRAIL 尾迹（含煤油储备款）抖动整体降频且随粒子年龄递减、老期幅度收敛；液氧煤油款寿命/黑烟/α 平方根淡出等行为零变化；WASH 烟浪不涉及 | 固体款 8 弹（9k720_9m723/bukm3_9m317ma/f14a_iriaf_aim23b/f14d_aim54c/irist_sl/j16_yj80/su57_kh38/su57_kh58）的飞行尾迹观感；服务端仅 NOOP 桥签名变化零行为 | 回退：holdTicks 恒传 0 + applyTrailCurve 恢复 0.45 寿命比分位即回定值保持；抖动恢复每 tick 重掷即回 20Hz。依据：`docs/plan/RVP弹道导弹尾迹HBM风格移植方案_20260915.md` 末节（2026-09-19 第三轮追加） |
