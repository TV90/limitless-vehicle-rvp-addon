# 修复方向总结：纯 Forge 服务端启动崩溃

> 状态：**阶段 1 已完成（服务端可启动）**；阶段 2（功能恢复）待推进
> 日期：2026-08-06
> 关联文档：[纯Forge服务端启动崩溃排查.md](../纯Forge服务端启动崩溃排查.md)（证据与机制细节）、
> [调试与修复规范.md](../调试与修复规范.md)（功能丢失记录表）

---

## 1. 问题

在纯 Forge 1.20.1 专用服务端（`D:\ywzj\Forge-test-server`）启动时，`ywzj_rvp` 与 `ywzj_vehicle`
两个 Mod 容器构造即崩溃：

```
java.lang.RuntimeException: Attempted to load class org/ywzj/vehicle/vehicle/LocalVehiclePlayer
for invalid dist DEDICATED_SERVER
    at org.ywzj.rvp.all.RVP_Entities.<clinit>(RVP_Entities.java:27)
    at org.ywzj.vehicle.all.AllEntities.<clinit>(AllEntities.java:160)
```

## 2. 根因（已确认）

- 本体 `ywzj_vehicle` 的**公共类**（`AmmoEntity`、`AbstractVehicle`、`WeaponUnit`、`RadarUnit`、
  `MissileEntity`、`InputHandler`、`ServerHitVehicleEvent` 等 20+ 个）在**公共方法**里直接引用
  `@OnlyIn(Dist.CLIENT)` 的 `LocalVehiclePlayer`（例：`AmmoEntity.readSpawnData` 用
  `LocalVehiclePlayer.instance.getPlayer()`）。
- 本体公共类自身**没有任何 `@OnlyIn` 成员** → 单独加载时不脏、不做帧重算 → `LocalVehiclePlayer`
  只是惰性引用（死代码），服务端永不解析 → **单独跑本体不崩**。
- RVP 的**公共数组 mixin**（约 39 个直接打带毒本体类：`AbstractVehicle` ×9、`WeaponUnit` ×22、
  `MissileEntity`/`RocketEntity`/`Radar`/`RadarUnit`/`InputHandler`/`ServerHitVehicleEvent` 等）
  使这些类变 dirty → Forge **强制重算栈映射帧** → 算帧解析到 `LocalVehiclePlayer` →
  RuntimeDistCleaner 拦截 `@OnlyIn(CLIENT)` 类加载 → **崩**。本体 `AllEntities.<clinit>:160`
  也是同一个机制（加载被 RVP mixin 改脏的实体类）。

## 3. 关键约束

- **本体 `ywzj_vehicle` 不能修改**（第三方 mod，仅作参考）。只能改 `ywzj_rvp`。
- 新纪律：非必要禁止 Mixin、禁止 `@Overwrite`（见 `agents.md`）。与下述方案 B 存在冲突，需特批。
- 服务端目标：纯 Forge 可启动，不依赖 Sinytra Connector。

## 4. 修复方向对比

| 方向 | 做法 | 效果 | 代价 / 冲突 |
|---|---|---|---|
| A. 修本体（作废） | 把本体公共字节码里的 `LocalVehiclePlayer` 引用挪进 `@OnlyIn(CLIENT)` 方法/桥接 | 根治，怎么改脏都安全 | **本体不能改，不可行** |
| A'. 最小公共 mixin 集（最快启动） | 从公共数组移除 5.1 表所列 28 个「改脏带毒类」的 mixin，只保留 5.2 的安全 mixin | 服务端立即可启动；改动仅为 `mixins.json` 排列，可逆、合规（减少 mixin） | **功能大量丢失**：联动 UAV、挂架部署、装甲/伤害系数、炮手数据、武器切换/装填/射击门控、传感器覆盖、雷达签名、锁定行为、着陆架手动覆盖、导弹 tick 扩展等全部失效——RVP 武器在服务端几乎退化为原版 |
| B. 定向去毒（功能全保） | 对 8 个带毒类的 16 个带毒方法用 `@Overwrite` 替换为双端安全实现：服务端安全逻辑/NOOP，客户端经公共接口桥接（`RVP_IClientActions`/`ClientActionsImpl`）走真实逻辑 | 能修好、功能全保、范围有界 | 必须用 `@Overwrite`（`@Inject`/`@Redirect` 会在 handler 签名残留 LVP 类型，照样崩）；违背禁 @Overwrite 纪律，需特批；需补全 client bridge |
| C. 渐进解绑（长期） | 按新纪律把功能从 mixin 改为事件/接口/网络/数据层，减少公共 mixin 对带毒本体类的依赖 | 符合纪律、长期稳健 | 工程量≈重做 RVP 大半功能，且部分功能无事件替代，**短期内服务端仍跑不了** |
| D. 不改 | 继续依赖 Sinytra Connector 跑服务端 | 零改动 | 服务端无法纯 Forge |

**结论**：本体不能修的前提下，能"修好崩溃 + 保留功能"的唯一现实路线是 **B（定向去毒）**；
C 是长期架构方向，不能作为本次修复手段；A' 可作为**阶段 1 快速验证**（证明"去掉改脏 mixin
服务端即可启动"，同时给用户一个可运行基线），但不作为最终形态。

**推荐路径（两阶段）**：
1. **阶段 1（A'）**：改动 `mixins.json` 排列，服务端先跑起来，功能丢失逐一记入
   `docs/调试与修复规范.md` 第 4 节表格。改动小、可逆、可立即验证机制。
2. **阶段 2（B，需特批）**：把 28 个 mixin 加回公共数组，同时新增去毒 mixin
   `@Overwrite` 16 个带毒方法（LVP → client bridge），恢复全部功能。

## 5. 修复范围清单（已精确到 8 类 16 方法）

「被 RVP 公共 mixin 改脏」∩「常量池引用 `LocalVehiclePlayer`」**精确交集 = 8 个本体公共类**。
每类只需处理**引用 LVP 的那几个方法**（带毒方法来自 `build/javap_out/poisoned_methods.txt`，
已用 `javap -c` 确认全部为惰性/死代码分支读取 LVP 单例，服务端永不执行）。

### 5.1 精确对照表：带毒类 × 公共数组 mixin × 带毒方法

| 带毒本体类 | 公共数组改脏它的 mixin（数量） | 带毒方法（重算帧时触发 LVP 加载） |
|---|---|---|
| `AbstractVehicle` | AbstractVehicleAps / EraState / GunnerChunkLoad / GunnerData / PhysicsOnlyCollision / LinkedUav / LauncherDeploy / HitboxDamageFactor（8 个） | `afterVehicleRot()`（protected）、`onLeaveVehicle(LivingEntity)`（public）、`setSeats(int[])`（private） |
| `WeaponUnit` | WeaponUnitAccessor / WeaponUnitArm / WeaponUnitSetWeapon / WeaponUnitExternalRadarLock / WeaponUnitLauncherDeployPoseBypass / WeaponUnitFollowParentRotation / WeaponUnitGroupedSlot / WeaponUnitPendingRadarLock / WeaponUnitSensorOverride / WeaponUnitShootDebug / WeaponUnitLauncherDeployGate / WeaponUnitModdingOnlyMulti / WeaponUnitSwitchWeapon（13 个） | `tickRemoteRot()`（protected）、`tickFireControl()`（public）、`fireControlLock()`（public）、`worldOwnerViewPosition(float)`（public） |
| `MissileEntity` | MissileEntityAccessor / MissileEntityMixin（2 个） | `updateOwner()`（private） |
| `RadarUnit` | RadarUnitMixin（1 个） | `tickDetect()`（public） |
| `Radar` | RadarSignatureMixin（1 个） | `getClientLevelEntities(Entity, AABB)`（public static）+ 2 个相关 lambda |
| `AbstractVehicleWeapon` | GunnerWeaponAccessorMixin（1 个） | `currentAimHitPosition()`（protected）、`onClientFire()`（public） |
| `ServerHitVehicleEvent` | ServerHitVehicleEventMixin（1 个） | `lambda$onServerMessageReceived$0`（private static） |
| `FixedWingVehicle` | AutoLandingGearManualOverrideMixin（1 个） | `tickSound()`（protected） |

合计 **28 个公共 mixin 参与改脏，16 个带毒方法**。

### 5.2 公共数组中的「安全 mixin」（目标不带毒，可原样保留）

`WeaponUnitPojo`、`WeaponUnitData`、`RadarUnitPojo`、`RadarUnitData`、`PartUnit`、`PartUnitType`、
`PartUnitData`、`SwitchableUnit`、`ControlUnit`、`VectorUtil`、`RocketEntity`、`VehicleExplosion`
（含 `GridCollectionTask`）、`VehicleDataManager`、`VehicleRocketWeaponData`、`ClientVehicleAction`、
`ClientRadarAction`、`Entity`（着陆架）、`RVP_BaseBullet`（RVP 自身）—— 均不在带毒名单，
**不触发崩溃**。

### 5.3 RVP 自身带毒公共类（需评估，暂不阻塞）

`RVP_WeaponBase`、`RVP_ExternalRadarLinkHelper` 常量池也含 LVP，但**引用全部位于
`@OnlyIn(Dist.CLIENT)` 方法内** → 服务端被 RuntimeDistCleaner 剥离方法后不参与帧重算，当前不崩。
待阶段 2 做实证复核（后续服务端运行到武器加载路径时验证）。

## 6. 待决策

- [ ] **走哪条路线**：
  - 路线 1（推荐）：阶段 1（A' 最小 mixin 集，先让服务端跑起来）→ 阶段 2（B 定向去毒恢复功能，需特批 `@Overwrite`）
  - 路线 2：直接跳阶段 1，一次性实施 B（功能全保，但工作量大，且需先特批）
  - 路线 3：只要 A' 结果（服务端能跑 + 接受功能丢失，长期再逐步恢复）
- [ ] 若走阶段 2：是否特批 `@Overwrite` 定向去毒（16 个带毒方法，LVP → client bridge）？
- [ ] 修复顺序与验证方式（构建 → 部署到 `Forge-test-server` → 验证启动）？
- [ ] 是否同步启动方案 C 的渐进解绑规划（不阻塞本次修复）？

## 7. 技术要点备忘

- 替换带毒方法**只能用 `@Overwrite`**：handler 签名里不得出现 `LocalVehiclePlayer` 类型。
- handler 内也不得直接引用任何 `@OnlyIn(CLIENT)` 类型（mixin 类被改脏重算帧时同样会崩）；
  客户端逻辑必须经**公共接口桥接**（`RVP_IClientActions`，实现类 `ClientActionsImpl` 仅在
  客户端 setup 时挂载，服务端永不加载）。
- 公共数组 mixin 的目标类必须双端安全；纯客户端行为放 `client` 数组。
- 本仓库调试禁止 HTTP 遥测；运行时证据用服务端日志/命令输出收集。

## 8. 阶段 1（A'）可执行步骤（待批准后动手）

1. 备份 `ywzj_rvp.mixins.json`。
2. 从 `"mixins"`（公共）数组移除 5.1 表所列 28 个 mixin（仅改 json 排列，不改源码）。
3. `gradlew build` → 部署新 jar 到 `Forge-test-server/mods/` → 启动验证 `Done (Xs)!`。
4. 将每项功能丢失记入 `docs/调试与修复规范.md` 第 4 节表。
5. 若服务端仍崩（说明还有漏网的改脏/带毒组合），用 `latest.log` 新崩溃栈反查继续精简。
