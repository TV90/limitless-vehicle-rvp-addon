# RVP 弹道导弹尾迹 HBM 风格移植方案（2026-09-15）

> 状态：已实现，待实机验收。
> 模仿对象：HBM（Hbm's Nuclear Tech Mod，1.7.10）弹道导弹尾迹
> `com.hbm.particle.ParticleRocketFlame`（飞行尾迹）+ 发射台 `launchSmoke`
> （`ParticleSmokePlume`，`TileEntityLaunchPad`）。
> 用户诉求：现有尾迹（原版营火烟 / `rvp_smoke` 灰烟团）观感不像弹道导弹，
> 参考 HBM 弹道导弹的"先火后烟"柱状尾迹重做。

## 一、HBM 原机制调研结论

- **飞行尾迹**（`EntityMissileBaseNT.spawnContraolWithOffset`，客户端每 tick）：
  沿上一 tick→当前位置的路径每 1 格 1 粒（每 tick 1~10 粒防高速断档），
  初速沿弹轴反方向 ×1.0，`ParticleRocketFlame` 渲染 10 层抖动 quad 伪体积感，
  >350 格剔除；无高空淡化门控，浓淡全程一致。
- **颜色/寿命/尺寸**（`ParticleRocketFlame`）：
  `dark = 1 - min(age/(maxAge*0.25), 1)`；R=dark+rand*0.3、G=0.6*dark+rand*0.3、
  B=rand*0.3（前 25% 寿命亮橙焰 → 深灰随机烟）；α=sqrt(1-age/maxAge)×0.75；
  每层半宽 `(rand*0.5 + 0.1 + 2*ageRatio) × scale`（约 0.35 → 2.6 × scale 膨胀）；
  寿命 60+rand(20)t；位置抖动幅度 `((ageRatio)*4+1)^1.5` 随寿命急剧扩大（柱状→散开云）；
  阻尼 0.91/tick、亮度 240 全亮。
- **大小弹差异**：`getContrailScale()` 小弹 0.5 / 大弹 1.0；Tier3/4 大弹 3~4 条平行尾迹（多喷口）。
- **发射台烟浪**（`TileEntityLaunchPad`）：导弹在发射台上方 AABB 内时每 tick 15 粒
  `ParticleSmokePlume`：寿命 80+rand(20)、0.25 → 2.25 线性膨胀、膨胀量转浮升、
  水平径向冲刷初速 0.6~0.9、阻尼 0.925、随机灰度。

## 二、RVP 实现（保留另一 AI 的尾迹基础设施，零 Mixin）

| 文件 | 改动 |
|---|---|
| `client/particle/RVP_RocketFlameParticle.java` | **新增**：TRAIL/WASH 双模式单类（沿用 `RVP_MchrFlareParticle` 模式型先例）。TRAIL=飞行尾迹（HBM 颜色/尺寸/α/阻尼公式逐条移植，3 层抖动 quad=10 层的性能折衷）；WASH=发射烟浪。贴图复用已迁移的 `textures/nuclear/particle_base.png`，独立 RenderType 全亮。 |
| `weapon/data/RVP_EffectsData.java` | 新增 `missile_native_trail_ground_wash`（null→按风格推导）+ `isMissileNativeTrailRocketFlame()` + `hasMissileNativeTrailParticleStyle()`（服务端广播跳过判据）。 |
| `client/bridge/RVP_IClientActions` / `RVP_ClientActions` / `RVP_ClientActionsAccess` | 新增桥 2 方法：`addRocketFlameTrailParticle`（带弹尾方向初速）、`addLaunchWashParticle`（初速内部随机径向）；服务端 NOOP。 |
| `entity/projectile/RVP_BaseBullet.java` | ① `spawnMissileNativeTrailParticles` 加 `rvp_rocket_flame` 分支（初速 = -lookAngle×1.0）；② 生成逻辑尾部加贴地烟浪块（燃烧 + 距地 <20 格，`getHeight(MOTION_BLOCKING)` 查地面投影，每 tick 6 粒）；③ `broadcastTrailParticles` 对配置了非空 style 的导弹整体跳过（服务端按 rvpData 判断，广播烟+硬编码尾焰一并抑制，两路视觉不再混合）。 |

## 三、移植参数对照表

| 参数 | HBM 原值 | RVP 落点 |
|---|---|---|
| 生成频率 | 1~10/tick 按位移、1 格 1 粒 | 复用 `missile_native_trail_step`（9M723 配 1.0）+ `spawn_interval_tick`/`density_scale` 现有字段 |
| 寿命 | 60+rand(20)t | 类内常量 60~80t |
| 初速/阻尼 | -弹轴×1.0、0.91/tick | 类内常量（初速由调用方按 -lookAngle 传入） |
| 尺寸 | (rand*0.5+0.1+2×ageRatio)×scale | 类内同式 × `missile_native_trail_particle_scale` |
| 颜色 | 前 25% 寿命橙焰 (1,0.6,0)→深灰 rand*0.3 | 类内同式（FLAME_PHASE_RATIO=0.25，逐 tick 掷随机保闪烁） |
| 透明度 | sqrt(1-age/maxAge)×0.75 | 类内同式 |
| 渲染 | 10 层抖动 quad 全亮、抖幅 ((4·ageRatio)+1)^1.5 | 3 层抖动 quad 全亮 + particle_base.png、抖幅同式 |
| 剔除 | 350 格 | 弹体实体追踪距离天然限制，不额外处理 |
| 大小弹差异 | scale 0.5/1.0、大弹 3~4 喷口 | `missile_native_trail_particle_scale`（多喷口本轮不做，未来可按出弹骨展开） |
| 发射烟浪 | 15/tick、80~100t、0.25→2.25、水平 0.6~0.9、浮升 | 6/tick、同寿命、0.25→2.25×scale、水平 0.5~0.9、膨胀转浮升；门控=燃烧 + 距地<20 格 |

## 四、9M723 定稿配置（载具包，不进 git）

```json
"effects_data": {
  "trajectory_particle": "none",
  "missile_native_trail_particle_style": "rvp_rocket_flame",
  "missile_native_trail_particle_scale": 1.5,
  "missile_native_trail_step": 1.0,
  "missile_native_trail_offset": 3.0
}
```

`ground_wash` 走风格默认（开启）；`projectile_data.flame_scale=1.15`（模型尾焰）保留。
已同步 4 份副本（仓库包 + run/client_1 + run/client_2 + run/server）。

## 五、行为变更与回归面

- **有意变更**：配置了非空 `missile_native_trail_particle_style` 的导弹不再走服务端广播尾迹
  （`trajectory_particle` + 燃烧期尾焰粒子）——9M723 是当前唯一配 style 的弹，其余武器行为不变；
  已记入 `docs/调试与修复规范.md` §4。
- **死字段澄清**：`missile_native_trail_extra_flame/_extra_smoke` 自 3b76306f 起无消费者，
  `docs/RVP导弹本体尾迹参数说明.md` 已标注"配置无效"（本轮未删除字段，保持 JSON 兼容）。

## 六、验证

- 编译：`./gradlew build` → 判据 BUILD SUCCESSFUL；
- 冒烟：`./gradlew runServer` → `server_smoke_rockettrail_20260915.log`，`Done (Xs)!` 判据，
  只看相对 §5.1 基线的新增错误；
- 实机清单（待回归）：
  - [ ] 9M723 发射：助推段弹尾"先火后烟"柱状尾迹（近弹尾细亮、远端粗散）；
  - [ ] 起飞贴地烟浪出现，爬升过 ~20 格后停止；冷发射点火前（ignition_delay 20t）无烟；
  - [ ] 熄火后尾迹停止（motor_burn_time=3000t 后）；双脉冲弹第二段点火尾迹恢复；
  - [ ] 广播叠加消失：9M723 弹道上只有一条本地尾迹（无"广播细烟"第二路）；
  - [ ] 其它导弹（AIM-9M 模板，无 style）尾迹回归不变（仍走广播+本地原版路径）；
  - [ ] 远距观瞄（>64 格）尾迹仍可见（弹体追踪距离内）。

## 七、回退

删除 `RVP_RocketFlameParticle` + 桥 2 方法 + `spawnMissileNativeTrailParticles` 的
rocket_flame 分支与烟浪块 + `broadcastTrailParticles` 的跳过判据 +
`RVP_EffectsData` 两个新访问器即恢复原状；9M723 JSON 把 style 改回 `rvp_smoke` 即回到灰烟观感。
